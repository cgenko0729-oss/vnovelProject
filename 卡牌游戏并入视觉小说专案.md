# 卡牌游戏并入视觉小说专案 — 合并实施计划

> **状态**：计划已定案，尚未开始施工（本文件撰写时未动任何一行程式码、未复制任何档案）
> **撰写日期**：2026-08-10
> **来源专案**：`D:\UnityAiProject\TryCardProject`（卡牌 roguelike）
> **目标专案**：`D:\UnityAiProject\MyProject`（视觉小说，本仓库 = 骨干与唯一真相）
> **目标**：视觉小说为主体，玩家在 VN 剧本流程中可以玩到**完整的卡牌游戏**

---

## 目录

- [一、可行性结论](#一可行性结论)
- [二、两专案体检结果](#二两专案体检结果)
- [三、实测冲突清单](#三实测冲突清单)
- [四、已定案的八项决策](#四已定案的八项决策)
- [五、目标目录布局](#五目标目录布局)
- [六、程序集依赖关系（关键架构约束）](#六程序集依赖关系关键架构约束)
- [七、分阶段实施计划](#七分阶段实施计划)
- [八、必须改动的卡牌程式码清单](#八必须改动的卡牌程式码清单)
- [九、剧本语法设计](#九剧本语法设计)
- [十、风险清单与保险丝](#十风险清单与保险丝)
- [十一、工作量估算](#十一工作量估算)
- [十二、被否决的方案与理由](#十二被否决的方案与理由)

---

## 一、可行性结论

**能复制大部分档案，但不能全部复制，而且「复制完 ≠ 整合完」。**

这次合并的底子比一般的专案合并好非常多，原因有三：

1. **两边技术栈几乎完全一致**（同 Unity 6、同 URP、同 Linear 色彩空间、同新版输入系统、Tag/Layer 档案逐字节相同）
2. **两边架构哲学一致**（都是「单场景 + 程序化搭建 UI」，卡牌场景里只有一个 `GameApp` 物件）
3. **卡牌专案有完整的 asmdef 组件化**，与 VN 的 Assembly-CSharp 之间天然形成**编译器强制的单向依赖**，方向正好是我们要的（VN 调用卡牌，卡牌看不到 VN）

真正的工作量不在「复制」，而在**把一个独立 App 改造成 VN 的子系统**：卡牌原本自带主选单、独占相机、独占音频、改全域 `Time.timeScale`，这些都必须变成「可被宿主开关与清理」的。

---

## 二、两专案体检结果

### 2.1 技术栈对比

| 检查项 | VN 专案 | 卡牌专案 | 结论 |
|---|---|---|---|
| Unity 版本 | 6000.5.3f1 | 6000.0.62f1 | 同 Unity 6，向前升级，安全 |
| 渲染管线 | URP 17.5.0 | URP 17.0.4 | 同管线；**URP 设定资产 GUID 完全相同**（同一套官方模板） |
| 色彩空间 | Linear | Linear | 一致 |
| 输入系统 | 仅新版（`activeInputHandler = 1`） | 仅新版 | 一致；卡牌还额外写了 `InputCompat` 兼容层 |
| Tag / Layer | — | — | `TagManager.asset` **两边逐字节相同** |
| DOTween | Plugins/Demigiant | Plugins/Demigiant | **`DOTween.dll` 两边逐字节相同** |
| 程式码规模 | 147 檔 / 43,717 行 | 202 檔 / 48,191 行 | 合并后约 9.2 万行 |
| 命名空间 | `VNEffects` | `Game.*` | 无冲突 |
| 同名型别 | — | — | 仅 `Entry` / `Option`，且各自在命名空间内，不冲突 |
| 组件化 | 无 asmdef（全在 Assembly-CSharp） | 4 个 asmdef | 见第六节 |
| 架构风格 | 单场景 + 程序化搭建 | 单场景 + 程序化搭建 | 哲学一致 |

### 2.2 卡牌专案的 asmdef 结构

```
Game.Runtime          references: []                       ← 纯逻辑，零外部依赖
Game.UI               references: [Game.Runtime, UnityEngine.UI, Unity.TextMeshPro,
                                   Unity.InputSystem, URP.Core, URP.Universal]
Game.Editor           references: [Game.Runtime, Game.UI, URP.*]   includePlatforms: [Editor]
Game.Tests.EditMode   references: [Game.Runtime, TestRunner]       includePlatforms: [Editor]
```

四个全部 `autoReferenced: true`（测试那个除外），这一点是整个合并方案的基础。

### 2.3 卡牌专案的资产体积

| 目录 | 体积 | 内容 |
|---|---|---|
| `Texture/` | 44 MB | AI 生成的卡面/角色/背景图、CardFrames |
| `Fonts/` | 13 MB | `LXGWWenKaiTC-Regular.ttf`（**与 VN 重复**） |
| `GameData/` | 6.6 MB | GameDatabase + 卡/敌/遗物/药水/事件 ScriptableObject + 5 张 JSON 表 |
| `Game/` | 2.5 MB | 202 个 `.cs` |
| `Docs/` | 694 KB | 7 篇架构文档 + WhatAiDo |
| `Tests/` | 311 KB | 16 个 EditMode 测试档 |
| `Resources/` | 44 KB | `CardFoil.shader` / `RoundedPanel.shader` / `GameVolumeProfile.asset` |
| `Scenes/` | 43 KB | `Main.unity` / `Battle.unity` / `SampleScene.unity` |

去掉重复字体后，实际搬运约 **54 MB**。

### 2.4 卡牌专案的运行时结构（读原始码确认）

- **入口**：`GameApp`（`Game/UI/GameApp.cs`，317 行）。场景里除了它什么都没有；`Awake` 里建 `RunManager` → `SaveService` → Canvas → TopBar → `Manager.GoToMainMenu()`
- **阶段机**：`RunPhase { MainMenu, Map, Battle, Reward, Shop, Event, Rest, Treasure, GameOver, Victory }`，`RunManager.PhaseChanged` 驱动换界面
- **渲染**：`UIRenderSetup` 惰性建一台 `UICamera`（正交、位置 `(0,5000,0)`），把它挂进场景现有主相机的 Camera Stack 当 Overlay，并把主相机设为 Base + 开后处理 + `volumeLayerMask = ~0`
- **音频**：`AudioService` 是 `DontDestroyOnLoad` 单例，会自动补 `AudioListener`
- **时间**：`TimeFeedback` 是 `DontDestroyOnLoad` 单例，致命一击的顿帧/慢放改的是**全域 `Time.timeScale`**
- **存档**：`SaveService` / `RunSave` 写 `Application.persistentDataPath`
- **设置**：`FeedbackSettings` 走 PlayerPrefs（震屏、闪光、慢放、战斗速度、音量），目前没有 UI
- **本地化**：自带 `Loc` + `LocaleTable`，目前只有 `Locale_en.asset`
- **字体**：`UIFactory` 用 `TMP_FontAsset.CreateFontAsset()` **运行时动态建**（先试 `Resources.Load<Font>("LXGWWenKaiTC-Regular")`，失败退回 OS 字体候选链）

### 2.5 VN 专案的事件模块契约（卡牌要接进去的地方）

`VNScriptRunner` 执行 `event` 命令时已经处理好了全部周边：

- `_eventActive = true` → **屏蔽 VN 的全部快捷键**（H 回想 / J 任务 / C 属性 / I 背包 / G 画廊 / 点击推进）
- 强制关 Auto / Skip
- `stage.dialogue.HideBox()`
- 从 `VNEventRegistry` 实例化模块到 `EventLayer`（Canvas 排序 60，介于 ChoicePanel 45 与 ScreenTransition 100 之间，**因此进出事件可以用全屏转场包住**）
- 挂起等 `Done(outcome)`
- 结果名匹配剧本的 `* 结果行` 做分支；整数结果同时写 `flag 事件结果`
- 结束后销毁模块、`dialogue.Show()`、记回想

这套契约正是为卡牌这种「暂停剧本 → 玩家交互 → 带结果返回」的玩法设计的，直接用。

---

## 三、实测冲突清单

逐个比对两边 693 / 622 个 `.meta` 的 GUID，**共 89 个 GUID 撞车**，全部集中在模板与插件档案上。

### 3.1 绝对不能复制的档案

| 不能复制的东西 | 撞车数 | 理由 | 处置 |
|---|---|---|---|
| `Assets/Plugins/Demigiant/`（DOTween） | 25 | GUID 全同；`DOTween.dll` 两边逐字节相同 | 用 VN 那份 |
| `Assets/TextMesh Pro/` | 45 | GUID 全同；两边只差 VN 多配了 fallback 字体 | 用 VN 那份 |
| `Assets/Settings/`（URP 5 个资产） | 8 | GUID 全同，卡牌 `GraphicsSettings` 指向的就是这个 GUID | **VN 那份自动生效** |
| `Assets/TutorialInfo/` + `Readme.asset` | 9 | Unity 模板残留 | 丢弃 |
| `Assets/Scenes/SampleScene.unity` | 1 | 模板场景 | 丢弃 |
| `InputSystem_Actions.inputactions` | 1 | GUID 同，VN 已有 | 用 VN 那份 |
| `Assets/Editor/GameViewFullscreen.cs` | 0（GUID 不同） | **但 `namespace FrameworkSample` + 类名完全相同 → 重复定义，编译直接红**；VN 那份较新（多了 Tools 开关） | 用 VN 那份 |

> 注：卡牌的 `Assets/Editor/` 底下**只有** `GameViewFullscreen.cs` 一个档，所以整个 `Assets/Editor/` 不需要搬。

### 3.2 隐性重复：字体

`LXGWWenKaiTC-Regular.ttf` 两边都有（VN 在 `Assets/Font/`，卡牌在 `Assets/Fonts/Resources/`，13 MB）。

卡牌是用 `Resources.Load<Font>("LXGWWenKaiTC-Regular")` 载入的，所以它必须落在某个 `Resources/` 底下。

**处置**：把 VN 那份**移动**到 `Assets/Font/Resources/`。
- 移动不改 GUID → VN 现有的 TMP 字体资产（`NotoSansSC-Dynamic` 等）引用不受影响
- 卡牌那份不复制 → 省 13 MB，且避免两份同名字体在专案里打架

### 3.3 需要搬运的清单

`Game/`、`GameData/`、`Tests/`、`Texture/`、`Docs/`、`Resources/`（3 个档）、`Scenes/Main.unity` + `Battle.unity`。

**这些全部零 GUID 冲突。**

### 3.4 写死路径

卡牌 Editor 工具里有 **40+ 处写死的 `"Assets/GameData/..."` / `"Assets/Scenes/..."` 路径**，分布在：

```
Game/Editor/CardTable/CardEditorWindow.cs        Game/Editor/CardTable/CardTableImporter.cs
Game/Editor/EnemyTable/EnemyEditorWindow.cs      Game/Editor/EnemyTable/EnemyTableImporter.cs
Game/Editor/RelicTable/RelicEditorWindow.cs      Game/Editor/RelicTable/RelicTableImporter.cs
Game/Editor/PotionTable/PotionEditorWindow.cs    Game/Editor/PotionTable/PotionTableImporter.cs
Game/Editor/EventTable/EventEditorWindow.cs      Game/Editor/EventTable/EventTableImporter.cs
Game/Editor/ContentValidator.cs                  Game/Editor/LocalizationTool.cs
Game/Editor/SampleContentGenerator.cs            Game/Editor/SampleContentCombinators.cs
Game/Editor/SampleContentCurses.cs               Game/Editor/TableImportUtil.cs
Game/Editor/MainSceneBuilder.cs                  Game/Editor/BattleSceneBuilder.cs
Game/Editor/VolumeProfileGenerator.cs
```

因为选定了「集中到 `Assets/CardGame/`」的布局，这 40+ 处必须改。处置方式见 P1。

---

## 四、已定案的八项决策

| # | 项目 | 决定 | 影响 |
|---|---|---|---|
| 1 | **程式码布局** | 集中到 `Assets/CardGame/` | 需改 40+ 处写死路径，抽成 `CardPaths` 常数类 |
| 2 | **卡牌专案去留** | 不再独立开发，**VN 专案成为唯一真相** | 不必考虑双向同步，可以放心重构路径 |
| 3 | **git 策略** | 单次提交，不搬历史 | 卡牌仓库原地保留当存档；VN 的 git log 保持干净 |
| 4 | **玩法粒度** | `mode:run`（完整一局）**+** `mode:battle`（单场战斗） | 同一模块两条路径；暂不做 VN 标题菜单入口，但 `CardGameSession` 设计成从任何地方都能起 |
| 5 | **数据联动** | 单向：`CardGameConfig` 注入 / `CardResult` 回传 | 顺着 asmdef 单向依赖，不破坏隔离 |
| 6 | **存档策略** | 卡牌保留自己的 `SaveService`（局中可存） | 需加「未完成局侦测 → 续玩/重开」 |
| 7 | **画面风格** | 卡牌保持自己的样子（专属 Volume + Layer） | 与单独跑时逐像素一致；进出用 VN 转场包住来吸收切换感 |
| 8 | **语言 / 设置** | VN 语言单向驱动卡牌 `Loc`；只接音量，其余设置不动 | 卡牌自己的语言切换 UI 在嵌入模式隐藏 |

---

## 五、目标目录布局

```
Assets/
├── CardGame/                       ★ 新增，整个卡牌子系统的边界
│   ├── Game/
│   │   ├── Game.Runtime.asmdef     ← 原样搬，纯逻辑
│   │   ├── Battle/  Cards/  Core/  Effects/  Enemies/  Events/  Map/
│   │   ├── Localization/  Potions/  Relics/  RunEffects/  Save/  Statuses/  Units/
│   │   ├── UI/       (Game.UI.asmdef)      ← 45 个档，含 GameApp
│   │   └── Editor/   (Game.Editor.asmdef)  ← 编表工具、场景生成器
│   ├── GameData/     6.6 MB  卡/敌/遗物/药水/事件资产 + 5 张 JSON 表 + GameDatabase
│   ├── Resources/    CardFoil.shader / RoundedPanel.shader / GameVolumeProfile.asset
│   ├── Scenes/       CardStandalone.unity / CardBattleDebug.unity（由 builder 重建）
│   ├── Texture/      44 MB
│   ├── Tests/        (Game.Tests.EditMode.asmdef) 16 个 EditMode 测试
│   ├── Docs/         卡牌自己的 7 篇架构文档 + WhatAiDo
│   └── Bridge/       ★ 全新接线层（不含 asmdef → 属于 Assembly-CSharp）
│       ├── VNCardGameModule.cs
│       └── Editor/VNCardGameInstaller.cs
└── Font/
    └── Resources/
        └── LXGWWenKaiTC-Regular.ttf   ← 由 Assets/Font/ 移入（移动不改 GUID）
```

**为什么 `Bridge/` 不放 asmdef**：`Bridge/` 在 `CardGame/` 底下但不在 `Game/` 底下，不被任何 asmdef 覆盖，于是自动归属 Assembly-CSharp —— 这正是它需要的位置（见下一节）。`Bridge/Editor/` 因为资料夹名叫 `Editor`，自动归属 Assembly-CSharp-Editor。

---

## 六、程序集依赖关系（关键架构约束）

Unity 的规则：**预定义程序集（Assembly-CSharp）自动引用所有 `autoReferenced: true` 的 asmdef**，反之不成立。因此：

```
┌───────────────────────────────────────────┐
│  Assembly-CSharp                          │
│  ├─ VN 全部程式码（VNEffects 命名空间）    │
│  └─ CardGame/Bridge/  ← 只有这一层同时看得到两边
└──────────────────┬────────────────────────┘
                   │ using Game.UI;  （零配置成立）
                   ▼
         ┌──────────────────┐
         │  Game.UI         │
         │  GameApp 等 45 檔 │
         └────────┬─────────┘
                  ▼
         ┌──────────────────┐
         │  Game.Runtime    │
         │  纯逻辑            │
         └──────────────────┘

✗ Game.UI / Game.Runtime 永远看不到 VNEffects、VNFlags、VNStage
```

### 由此导出的硬性规则

| 类别 | 必须放哪 | 理由 |
|---|---|---|
| `CardGameConfig`（VN → 卡牌 的注入契约） | **`Game/UI/`（Game.UI 内）** | `GameApp` 要用它 |
| `CardResult`（卡牌 → VN 的回传契约） | **`Game/UI/`（Game.UI 内）** | `GameApp` 要产出它 |
| `CardGameSession`（建/清 GameApp 的门面） | **`Game/UI/`（Game.UI 内）** | 全部操作卡牌侧物件 |
| `VNCardGameModule`（`VNEventModule` 子类） | **`Bridge/`（Assembly-CSharp）** | 需同时 `using VNEffects` 与 `using Game.UI` |
| `VNCardGameInstaller`（Editor 装场景工具） | **`Bridge/Editor/`** | 需同时看到 `VNEventRegistry` 与模块类 |

> **常见错误**：把 `CardGameConfig` 放进 `Bridge/`。那样 `GameApp` 会编译不过，而错误讯息（「找不到型别或命名空间 CardGameConfig」）不会告诉你真正的原因是程序集方向。

---

## 七、分阶段实施计划

每一阶段都有明确验收标准，做完可以停下来验证再往下走。全程在分支 `agent/card-game-merge` 上进行（照技能 `vn-new-feature` 的流程）。

### P0 前置（约 0.5 小时）

1. **卡牌专案先 commit 干净**。目前有大量未提交改动：
   - `Assets/Docs/ProjectUseGuide.md`
   - `Assets/Game/Core/GameDatabase.cs`
   - `Assets/Game/UI/BattleScreen.cs`、`UIFactory.cs`、`UnitView.cs`
   - 上百个 `Assets/GameData/Cards/Card_*.asset`

   不先 commit 的话，搬过来的是一个半成品状态，且**无法回溯到「合并前的卡牌是什么样」**。
2. VN 专案 commit 现有改动（目前有一批 `.claude/skills/*` 的修改）。
3. VN 开分支 `agent/card-game-merge`。

**验收**：两边 `git status` 干净。

---

### P1 搬运（约 2 小时）

1. 建立 `Assets/CardGame/`，复制第 3.3 节列出的资料夹（**连 `.meta` 一起复制**，否则 GUID 全部重生成，所有资产引用断链）。
2. **跳过**第 3.1 节的 7 类档案。
3. 字体去重：`Assets/Font/LXGWWenKaiTC-Regular.ttf` → `Assets/Font/Resources/`（用 Unity 内的移动，或直接连 `.meta` 一起移动）。
4. 新增 `Game/Core/CardPaths.cs`：
   ```csharp
   public static class CardPaths
   {
       public const string Root      = "Assets/CardGame";
       public const string GameData  = Root + "/GameData";
       public const string Scenes    = Root + "/Scenes";
       public const string Resources = Root + "/Resources";
       // …各表的 JSON 路径、Authored 子目录等
   }
   ```
   把第 3.4 节列出的 19 个档里的写死路径全部改成常数拼接。
5. `MainSceneBuilder.ScenePath` → `CardPaths.Scenes + "/CardStandalone.unity"`；
   `BattleSceneBuilder.ScenePath` → `CardPaths.Scenes + "/CardBattleDebug.unity"`。
6. 检查 `.gitignore`，确认 44 MB 的 `Texture/` 不会被误忽略。

**验收**
- Unity 重开后 **Console 零 error**
- `Tools/卡牌游戏/*` 菜单全部在
- `Game.Tests.EditMode` **16 个测试档全绿**

---

### P2 独立可跑（约 1 小时）

1. 跑 `Tools/卡牌游戏/1. 生成示例内容`，确认编表工具在新路径下正常读写。
2. 跑 `Tools/卡牌游戏/6. 生成后处理配置`，确认 `GameVolumeProfile.asset` 落在 `CardGame/Resources/`。
3. 用两个 SceneBuilder 在 `Assets/CardGame/Scenes/` 重建场景。

**验收**：在 VN 专案里打开 `CardStandalone.unity` 按 Play，**卡牌游戏完整可玩、画面与原专案一致**。

> 这是一个干净的中间检查点：此时卡牌与 VN 还完全无关，出问题不会牵连 VN 的任何东西。即使后面暂停，专案也停在一个自洽的状态。

---

### P3 嵌入外壳（约 1.5~2 天，本次真正的技术难点）

#### 新增档案（全部在 `Game/UI/` 内，属于 Game.UI）

**`CardGameConfig.cs`**
```
Mode                 Run | Battle
EncounterId          Battle 模式的敌人 id
StartingHp / MaxHp   起始生命
StarterRelicId       起始遗物
StarterDeck          起始牌组
Seed                 0 = 随机
Language             由 VN 的 VNLocale 传入
MasterVolumeScale    由 VN 的基准音量传入
Embedded             true = 嵌入模式（跳主选单、隐藏语言切换与退出）
RunId                本次事件的唯一 id，用于存档续玩侦测
```

**`CardResult.cs`**
```
Outcome              Victory | Defeat | Escape | Abandoned
RemainingHp          结束时剩余生命
Gold                 结束时金钱
FloorReached         走到第几层
RunId
```

**`CardGameSession.cs`** —— 静态门面，`Begin(config, onFinished)` / `Abort()`，负责：
1. 快照 VN 主相机的 `renderPostProcessing` / `volumeLayerMask` / `cameraStack`
2. 在**场景根节点**建 `GameApp`（不是 VN Canvas 底下，见第十节坑 3）
3. 注入 config、订阅 `Finished`
4. 结束时执行**完整清理**（见下）

#### 清理清单（`CardGameSession` 退出时必须逐项执行）

| # | 清理项 | 不做的后果 |
|---|---|---|
| 1 | 从 VN 主相机 `cameraStack` 移除 UICamera 并销毁它 | stack 累积 null，URP 报错 |
| 2 | 还原 VN 主相机的 `renderPostProcessing` / `volumeLayerMask` | VN 画面被静默改掉，且不报错 |
| 3 | 销毁卡牌专属 Volume 与其 Layer 上的物件 | 残留影响 VN 调色 |
| 4 | `Time.timeScale = 1f` | 顿帧中途退出 → **VN 整个冻结** |
| 5 | 停 `AudioService` 的 BGM 并销毁 `~AudioService` 单例 | 卡牌音乐在 VN 剧情里继续放 |
| 6 | 销毁 `~TimeFeedback` 单例 | 残留物件 + timeScale 风险 |
| 7 | `Destroy(GameApp 根)` | — |

#### 改动档案清单

见第八节。

**验收**：在 VN 场景挂一个临时测试按钮，**反复进出卡牌 5 次**：
- Hierarchy 里无残留（`~AudioService` / `~TimeFeedback` / `UICamera` / `GameApp` 全消失）
- Console 零 error 零 warning
- VN 画面与进入前**逐像素一致**（用截图比对）
- `Time.timeScale == 1`

---

### P4 接进剧本（约 1 天）

#### 新增（在 `Bridge/`）

**`VNCardGameModule.cs : VNEventModule`** —— 只做四件事：
1. 读 `ctx.kwargs` 组 `CardGameConfig`（数值支援 `[flag名]` 内插，走 VN 现成的 `VNParameterInterpolator`）
2. 用 VN 现成的 `VNScreenTransition` 遮住进入
3. `CardGameSession.Begin(...)`，挂起等待
4. 结果写 flag 后 `Done(结果名)`

**`Editor/VNCardGameInstaller.cs`** —— 照 VN 现有的 `VNQuizInstaller` / `VNLiquidInstaller` 的写法做**增量装场景**（补一个禁用的模板物件 + 登记进 `VNEventRegistry`），可重复执行、不重建场景。

> 注意 `VNEventRegistry.Create()` 会把模板 Instantiate 成 `EventLayer` 的 `RectTransform` 子物件，所以**模板物件必须带 `RectTransform`**（这是 `VNQuizInstaller` 已经踩过的坑）。

#### 还要接的三处（VN 的既有规矩）

1. **剧本编辑器 Schema 登记**（`VNScenarioSchema`）—— 走技能 `vn-editor-extend`
2. **静态校验器 Lint 规则** —— 检查 `mode:` 合法、`enemy:` 存在于 GameDatabase、`* 结果行` 名称合法
3. **续玩侦测** —— 进入模块时若发现同 `RunId` 的未完成卡牌存档 → 弹「继续上次 / 重新开始」

> 第 3 点的必要性：玩家在卡牌局中存了卡牌档、直接关游戏，下次从 VN 档读回来时，若不侦测会出现「VN 以为还没打，卡牌以为打到一半」的分裂状态。

**验收**
- 剧本 `event 卡牌对战` 跑通，`* 结果行` 分支正确
- Auto / Skip 被正确屏蔽（Runner 的 `_eventActive` 已处理，只需确认）
- 回想（Backlog）记一条
- 事件**前后**可正常存读 VN 档

---

### P5 收尾（约 0.5 天）

1. 存档兼容三处同步 —— 走技能 `vn-save-compat`（`VNSaveData` 字段 / `RestoreSnapshot` / `RebuildStateBefore`）
2. 文档 —— 走技能 `vn-doc-update`：
   - `WhatAiDo.md` 新章节（本次合并的完整记录）
   - `CLAUDE.md` 组件速查表加一行 `VNCardGameModule`
   - `CLAUDE.md` 子系统表加一行「卡牌游戏」
   - `HowToUse.md` 加 `event 卡牌对战` 语法
   - `ProjectCodeGuide.md` 加 CardGame 分区说明
3. 合并回 `main`，**不删分支**（专案规矩）

---

## 八、必须改动的卡牌程式码清单

卡牌原本是一个**独立 App**，要变成 VN 的**子系统**，以下每一处都是读过原始码确认必要的。

| # | 位置 | 现况 | 要做什么 | 难度 |
|---|---|---|---|---|
| 1 | `Game/UI/GameApp.cs` | `Awake` 直接 `Manager.GoToMainMenu()`，没有「结束」出口 | 接受 `CardGameConfig` 注入；`Embedded` 时跳过主选单直接进 Map（Run）或 Battle；`GameOver` / `Victory` 阶段触发 `Finished(CardResult)` 而不是停在结算画面 | 中 |
| 2 | `Game/UI/UIRenderSetup.cs` | 静态惰性 `Build()`，**会就地改 VN 主相机的三个属性**，且只有 Build 没有 Teardown | 新增 `Teardown()`：移出 cameraStack、销毁 UICamera、还原快照 | 中 |
| 3 | `UIRenderSetup.EnsureVolume()` | `FindFirstObjectByType<Volume>()` 找到就不建 —— 在 VN 里**永远会找到 `VNMoodGrading` 的 Volume**，于是卡牌自己的 Bloom 配置完全不生效 | 嵌入模式下**总是**建卡牌专属 Volume，挂专属 Layer；UICamera 的 `volumeLayerMask` 收窄到该层 | 中 |
| 4 | `Game/UI/TimeFeedback.cs` | 顿帧/慢放改**全域** `Time.timeScale` | 退出时强制 `Time.timeScale = 1f` 并销毁单例 | 低 |
| 5 | `Game/UI/AudioService.cs` | `DontDestroyOnLoad` 单例，自动补 `AudioListener` | 退出时停 BGM + 销毁单例；音量 = 卡牌音量 × `config.MasterVolumeScale` | 低~中 |
| 6 | `Game/UI/MainMenuScreen.cs` + 设置入口 | 有自己的主选单、语言切换、退出游戏 | 嵌入模式隐藏语言切换与「退出游戏」 | 中 |
| 7 | `Game/UI/InputCompat` 的 Esc 流 | Esc 回卡牌主选单 | 嵌入模式改为「放弃本局」确认框 → `Outcome.Abandoned` | 低 |
| 8 | `Game/Localization/Loc.cs` | 语言从 `MetaSave` 读 | 嵌入模式改由 `config.Language` 驱动；缺译退回中文 | 低 |
| 9 | `Game/Core/SaveService.cs` | 存 `persistentDataPath`，与 VN 无关 | 加 `RunId` 字段；加「查询是否存在未完成局」API | 中 |
| 10 | 19 个 Editor 档的写死路径 | `"Assets/GameData/..."` 等 40+ 处 | 抽 `CardPaths` 常数类（P1 完成） | 低（量大） |

---

## 九、剧本语法设计

沿用 VN 现有 `VNBattleModule` 的参数惯例（`phpstat:` 从 flag 读属性、结束写 flag）。

### 完整一局 roguelike

```
event 卡牌对战 mode:run hp:80 relic:burning_blood seed:0
* 胜利
    jump 击败魔王
* 失败
    jump 败北结局
* 放弃
    jump 临阵脱逃
```

### 剧情指定的单场战斗，起始 HP 读 VN 养成属性

```
event 卡牌对战 mode:battle enemy:guardian hpstat:体力
* 胜利
* 失败
* 逃跑
```

### 参数表

| 参数 | 适用模式 | 说明 |
|---|---|---|
| `mode:` | 两者 | `run` / `battle`，必填 |
| `enemy:` | battle | 敌人 id（`slime` / `double_slime` / `jawworm` / `mixed` / `guardian`…） |
| `hp:` | 两者 | 起始生命固定值 |
| `hpstat:` | 两者 | 起始生命读自 VN flag（与 `hp:` 二选一） |
| `relic:` | run | 起始遗物 id |
| `deck:` | run | 起始牌组，如 `strike*5,defend*4,bash*1` |
| `seed:` | 两者 | `0` = 随机；非 0 = 固定种子（复现用） |

### 回传写入的 flag

| flag | 内容 |
|---|---|
| `卡牌结果` | 胜利 / 失败 / 逃跑 / 放弃 |
| `卡牌剩余HP` | 结束时剩余生命 |
| `卡牌金钱` | 结束时金钱 |
| `卡牌层数` | 走到第几层（run 模式） |

外加 VN Runner 既有行为：整数结果自动写 `flag 事件结果`。

---

## 十、风险清单与保险丝

### 坑 1：相机堆叠污染（最高风险）

`UIRenderSetup.Build()` 会把 VN 主相机强制设为 Base、开 `renderPostProcessing`、`volumeLayerMask = ~0`，并把 UICamera 塞进它的 stack。

**不还原的后果**：VN 的画面会在你察觉不到的地方变了 —— `VNMoodGrading` 的双 Volume 权重交叉过渡可能被扰动，而且**不报任何错**。

**保险丝**：P3 必须做快照 / 还原，并用截图逐像素比对验收。

### 坑 2：全域 `Time.timeScale`

卡牌致命一击的顿帧会把 `Time.timeScale` 压到 0。若玩家恰好在那一刻按了「放弃本局」，退出后 VN 会**整个冻结**（VN 的 Tween 大量走 scaled time）。

**保险丝**：`CardGameSession` 的清理清单第 4 项，无条件 `Time.timeScale = 1f`。

### 坑 3：巢状 Canvas

`VNEventRegistry.Create()` 会把模块 Instantiate 到 VN Canvas 底下。而 `GameApp` 自己也建 Canvas —— **巢状 Canvas 会静默忽略自己的 `renderMode` 和 `CanvasScaler`**，被迫继承 VN 根 Canvas。卡牌 UI 的尺寸推导会全错。

**保险丝**：把模块设计成「遥控器」—— `VNCardGameModule` 本身留在 EventLayer（只负责挂起等待与转场），真正的 `GameApp` 建在**场景根节点**。

### 坑 4：44 MB 贴图进公开仓库

VN 是公开的 GitHub 仓库（`https://github.com/cgenko0729-oss/vnovelProject.git`），卡牌 `Texture/` 里有 AI 生成的角色/背景图。

**需要确认**：这些素材有没有授权顾虑。这一项只有专案拥有者能判断。

### 坑 5：两套字体管线并存

VN 走 `VNFont` + 预烘焙 TMP 资产；卡牌走 `TMP_FontAsset.CreateFontAsset()` **运行时动态建**。

共存没有技术问题，但：
- 运行时建字体资产有一次性卡顿（首次进卡牌时）
- 两边字型可能不完全一致

本次决策是「卡牌保持自己的样子」，所以**不处理**，只在文档里记录。

### 坑 6：`.meta` 必须一起复制

复制资产时若漏掉 `.meta`，Unity 会重新生成 GUID，导致 `GameDatabase` 对上百个卡牌资产的引用**全部断链**，且表现为「资产还在但游戏说找不到内容」。

**保险丝**：P1 用档案系统整目录复制（含隐藏的 `.meta`），复制后立刻检查 `GameDatabase.asset` 的引用是否完好。

---

## 十一、工作量估算

| 阶段 | 时间 | 风险 | 可独立验收 |
|---|---|---|---|
| P0 前置 | 0.5 h | 无 | ✅ 两边 git 干净 |
| P1 搬运 | 2 h | 低（机械劳动） | ✅ 编译零错 + 16 个测试全绿 |
| P2 独立可跑 | 1 h | 低 | ✅ 卡牌在 VN 专案里能单独玩 |
| P3 嵌入外壳 | 1.5~2 d | **高** | ✅ 进出 5 次不残留、VN 画面不变 |
| P4 接剧本 | 1 d | 中 | ✅ `event 卡牌对战` 跑通 |
| P5 收尾 | 0.5 d | 低 | ✅ 存档兼容 + 文档齐 |

**总计约 3~4 个工作日。**

**建议的停点**：P2 做完就有一个「卡牌在 VN 专案里能独立玩」的稳定状态，即使在此暂停也不会留下半吊子。

---

## 十二、被否决的方案与理由

### 程式码布局

| 方案 | 为什么否决 |
|---|---|
| **A1 原路径复制**（`Assets/Game`、`Assets/GameData`… 放 VN 顶层） | 优点是零路径改动、与独立卡牌专案 diff 永远干净。但既然决策 2 是「卡牌专案不再开发」，同步成本归零，整洁度就压倒一切。VN 顶层已经有 `Art/Audio/Project/Scenarios`，再塞 6 个卡牌资料夹会很乱，而且 `Assets/Scenes` 与既有的 `Assets/Project/Scenes` 会让人困惑 |
| **A3 做成本地 UPM package** | package 内的资产在 Editor 里是**唯读**的，而卡牌有一整套要往 `GameData` 写资产的 Editor 编表工具（CardEditorWindow、各种 Importer、SampleContentGenerator）—— 会全部失效，等于把整套内容制作流程砍掉 |

### 运行时宿主

| 方案 | 为什么否决 |
|---|---|
| **B1 切场景**（`SceneManager.LoadScene`） | VN 是单场景架构，整个舞台（背景/立绘/粒子/运镜/对话框/存档状态）都在场景里程序化搭建，切走再切回等于**全部重建**，还要处理 Runner 的续跑。代价极高、收益为零 |
| **B2 Additive 场景叠加** | 相机 / Canvas / EventSystem / AudioListener 仍然共用，隔离度并不比 B3 高，反而多了非同步载入的时序问题 |
| **B3 做成 VN 事件模块**（采用 ✅） | VN 已有为此设计的完整契约（`VNEventModule` + `VNEventRegistry` + Runner 的 `_eventActive` 输入闸门 + 结果分支 + 回想记录），零额外基建 |

### 其他

| 方案 | 为什么否决 |
|---|---|
| **git subtree 保留卡牌历史** | 要先重写卡牌仓库的路径，操作繁琐，且 VN 的 git log 会被数十个卡牌 commit + 10 多个 feature 分支淹没。卡牌仓库原地保留就能随时查历史 |
| **两套本地化合并成一套** | 把卡牌的 `LocaleTable` 全部迁进 `VNLocale` 是一项独立的大工程，与本次合并耦合会让 P4 无限膨胀。先做单向驱动，将来有需要再单独做 |
| **卡牌进度并入 VN 存档** | 要打通两套完全不同的序列化系统（VN 的 `VNSaveData` vs 卡牌的 `RunSave` + 迁移机制），工作量最大。卡牌自带的 `SaveService` 已经能让玩家在长局中存档，实际体验差距很小 |

---

## 附录：验收检查表（施工时逐项打勾）

### P1
- [ ] `Assets/CardGame/` 下 7 个资料夹到位，`.meta` 齐全
- [ ] 7 类冲突档案确认未复制
- [ ] `Assets/Font/Resources/LXGWWenKaiTC-Regular.ttf` 就位，VN 的 TMP 字体资产未断链
- [ ] `CardPaths.cs` 建立，19 个 Editor 档路径全改完
- [ ] Unity Console 零 error
- [ ] `Tools/卡牌游戏/*` 菜单全在
- [ ] `Game.Tests.EditMode` 16 个测试档全绿

### P2
- [ ] `Tools/卡牌游戏/1. 生成示例内容` 正常
- [ ] `GameVolumeProfile.asset` 落在 `CardGame/Resources/`
- [ ] `CardStandalone.unity` 按 Play 完整可玩
- [ ] `CardBattleDebug.unity` 按 Play 可直接开打
- [ ] 画面与原专案一致（截图比对）

### P3
- [ ] `CardGameConfig` / `CardResult` / `CardGameSession` 在 `Game/UI/` 内
- [ ] `UIRenderSetup.Teardown()` 实作完成
- [ ] 卡牌专属 Volume + Layer 生效
- [ ] 反复进出 5 次：Hierarchy 无残留
- [ ] 反复进出 5 次：Console 零 error 零 warning
- [ ] 退出后 VN 画面逐像素一致
- [ ] 退出后 `Time.timeScale == 1`
- [ ] 顿帧中途强制退出，VN 不冻结

### P4
- [ ] `VNCardGameModule` 在 `Bridge/`
- [ ] `VNCardGameInstaller` 可重复执行、模板带 `RectTransform`
- [ ] `event 卡牌对战 mode:battle` 跑通，三个结果分支正确
- [ ] `event 卡牌对战 mode:run` 跑通
- [ ] `hpstat:` 从 VN flag 正确读值
- [ ] 回传 flag 正确写入
- [ ] Auto / Skip 被屏蔽
- [ ] 回想记一条
- [ ] 事件前后可正常存读 VN 档
- [ ] 未完成局侦测：关游戏再读档，正确询问续玩/重开
- [ ] 剧本编辑器 Schema 登记完成
- [ ] Lint 规则生效

### P5
- [ ] `VNSaveData` / `RestoreSnapshot` / `RebuildStateBefore` 三处同步
- [ ] `WhatAiDo.md` 新章节
- [ ] `CLAUDE.md` 组件表 + 子系统表
- [ ] `HowToUse.md` 语法说明
- [ ] `ProjectCodeGuide.md` CardGame 分区
- [ ] 合并回 `main`，分支保留
