# 横版无限跑酷小游戏实施计划

> **状态**：三轮问答已定案 **11 项**，仅剩「金币素材路径」一项待补（见 3.1），可随时开工
> **撰写日期**：2026-09-03
> **目标**：VN 剧本流程中可以 `event runner` 拉起一段「自动向左跑 → 三车道闪避 + 跳跃滑铲
> → 吃金币」的横版跑酷，跑完指定距离过关，成绩回流剧情分支
> **建议分支**：`feature/runner-minigame`（用户发话后再切）

---

## 目录

- [一、玩法定性与设计风险](#一玩法定性与设计风险)
- [二、已定案的十一项决策](#二已定案的十一项决策)
- [三、仍未确定的事](#三仍未确定的事)
- [四、参数数学（难度是可以算出来的）](#四参数数学难度是可以算出来的)
- [五、技术方案](#五技术方案)
- [六、文件清单](#六文件清单)
- [七、资产字段设计](#七资产字段设计)
- [八、剧本语法设计](#八剧本语法设计)
- [九、分阶段实施计划](#九分阶段实施计划)
- [十、风险清单与保险丝](#十风险清单与保险丝)
- [十一、被否决的方案与理由](#十一被否决的方案与理由)
- [十二、工时估算](#十二工时估算)

---

## 一、玩法定性与设计风险

### 1.1 跑酷的乐趣内核是「读谱」，不是「反应」

三车道跑酷玩家真正在做的事，是**提前 1~2 秒读出前方的图案，规划一条连续路线**——
先上后中再下、这个矮障碍跳过去顺手串一排金币。「看到障碍才躲」是失败态，不是玩法。

由此推出两条硬约束，全篇设计都绕着它转：

1. **前方可视距离必须换算成「秒」**，而不是「像素」。速度越快，同样的屏幕宽度给的
   反应时间越短——所以速度上限不是随便填的数，是被「前视距离 ÷ 最短可接受反应时间」
   算出来的（见 4.1）。
2. **关卡必须可解**。随机生成最容易出的事故是「三条车道同时被堵」「刚换到上车道，
   下一帧上车道就出现障碍」「跳过矮障碍，落地点正好是全高障碍」。
   这不是难，是无解——玩家会认为游戏坏了。

### 1.2 ★ 已确认的设计选择：向左跑

横版跑酷 99% 向右跑（马里奥、Subway Surfers、Canabalt）。本作**刻意向左**，
用于制造「被追赶 / 逃离」的压迫感。`runDirection` 仍做成 Def 字段，
默认 `Left`，将来想试向右不用改代码。

### 1.3 ★ 车道与跳跃的轴冲突（已解决，但实现时不能忘）

Subway Surfers 的车道是**横向**（X 轴）、跳跃是**纵向**（Y 轴），两轴正交所以互不干扰。
本作是横版侧视，**车道已经占用 Y 轴**——再加跳跃，跳跃也在 Y 轴上，
「在上车道跳起来」与「换到更高的车道」在画面上有混淆风险。

**解法（已定案）：车道内起跳 + 地面影子**，靠四重视觉区分：

| 区分手段 | 换道 | 跳跃 |
|---|---|---|
| Y 位移量 | 180 px（整条车道间距） | **120 px**（刻意小于 180，永不跨进邻道） |
| 运动曲线 | 直线平移 | 抛物线，有滞空 |
| 时长 | 0.14 s | 0.29 ~ 0.67 s（随速度，见 4.6） |
| **地面影子** | 影子跟着换道线一起走 | **影子留在原车道线上**，只缩小变淡 |

其中**地面影子是决定性的那一条**——它是「你现在到底在哪条车道」的唯一权威答案。
没有影子，这套设计不成立。

### 1.4 与 VN 剧情的关系

跑酷是**纯玩法模块**，遵守模块三铁律，不碰舞台演出（不同于 aitalk / interact
那两个刻意破例的模块）。角色出场、背景、情绪全交给事件前后的剧本行。
模块只操作自己的 UI 子树 + VNFlags。

---

## 二、已定案的十一项决策

| # | 决策 | 内容 |
|---|---|---|
| 1 | **结束条件** | **跑到指定距离 / 时长过关**。有终点，有进度条。结果三档 `完美 / 通关 / 失败` |
| 2 | **课程生成** | **手写 chunk 随机拼接**（主力）+ **固定顺序预置关卡**（剧情关 / 教程关）。纯参数随机撒点不做 |
| 3 | **角色表现** | **先程序化占位**，表现层集中在 `VNRunnerActor.cs` 一个文件，素材到了只改它 |
| 4 | **金币动画** | **不用 Animator**，走自写 `VNSpriteFlipbook`（理由见 5.3） |
| 5 | **跑动方向** | **向左**（刻意），`runDirection` 仍做成 Def 开关 |
| 6 | **跑道呈现** | **纯平面侧视**，三条等距水平线，物件不按车道缩放。不做伪透视 |
| 7 | **完美档判定** | **全程不掉血**。金币只影响属性换算与 flag，不影响档位 |
| 8 | **撞击惩罚** | **扣 1 命 + 1.2 秒无敌闪烁，速度不变**。不做速度归零、不做击退 |
| 9 | **动作扩展** | **做跳跃 + 滑铲**。不做拾取道具，不做养成属性联动（`stat:`/`rate:` 结算换算仍保留） |
| 10 | **障碍类型** | **三类**：矮障碍（跳过）/ 高障碍（滑过）/ 全高障碍（只能换道） |
| 11 | **按键与空中控制** | **W/S 换道、Space 跳、Shift 滑**；**空中与滑铲中不能换道，落地才能换**（配输入缓冲，见 5.9） |

### 决策 1 带来的两个连锁结论

- **有终点 = 难度曲线有终点**。速度上限按「一局时长」倒推，不需要无尽模式那种渐近曲线。
- **进度条是必需 UI**，不是加分项。玩家要知道还剩多远，才能决定要不要冒险抢金币。

### 决策 2 带来的架构简化（重要）

chunk 是**手写过的**，所以「可解性」由作者在写 chunk 时保证，
生成器只需要保证**接缝处可解**——前一个 chunk 的末尾与后一个的开头之间，
留出 ≥ 一次换道所需的距离。这把 1.1 里那个最麻烦的问题从「运行时图搜索」
降级成「拼接时插入 N 格空白」。

### 决策 11 带来的一条必须做的手感补偿

「空中不能换道」意味着玩家在空中按 W 会**毫无反应**，高速下这是很挫败的体验。
必须配 **0.15 秒输入缓冲**：空中按下的换道请求存起来，落地那一帧立即执行。
见 5.9。**这一条不做，决策 11 就是个 bug 而不是设计。**

---

## 三、仍未确定的事

### 3.1 金币 sprite 在哪？

工程里 `find Assets -iname "*coin*"` 零命中，也没有任何 `spriteMode: 2`（Multiple）
的 sheet。请给路径，或确认「还没进工程」——P1 会用程序化金币先跑通，不阻塞开发。

同时告诉我形态，这决定要不要写「自动切帧」工具：

| 形态 | 处理 |
|---|---|
| 已切好的 Multiple sprite sheet | 最省事，Def 里多选拖入即可 |
| 一整张未切的横向条带 | 我加个「按帧数自动切」的编辑器按钮 |
| 多张单帧 png | 直接多选拖进 List |

> 注意 `VNTextureImportDefaults` 会把白名单目录里首次导入的图设成
> `Sprite (2D and UI) / Single`。金币 sheet 要手动改成 `Multiple` 再切——
> 手调过的设置不会被打回（`importSettingsMissing` 已为 false）。

### 3.2 P1 跑起来后必然要重调的手感参数

这些我给了有依据的默认值，但**手感只能玩过才知道**，都放在 `VNRunnerTuneWindow` 里：
跳跃距离、滑铲时长、输入缓冲窗口、判定盒宽容度、速度上限。

---

## 四、参数数学（难度是可以算出来的）

设计分辨率 **1920×1080**（同项目 Canvas）。

### 4.1 反应时间 = 前视距离 ÷ 速度

玩家 X 固定在 **x = 480**（屏幕四分之一处），前方留 **1440 px** 的可视距离。

| 速度 | 反应时间 | 手感 |
|---|---|---|
| 600 px/s | 2.40 s | 轻松，教学期 |
| 900 px/s | 1.60 s | 舒适 |
| 1200 px/s | 1.20 s | 紧张 |
| **1400 px/s** | **1.03 s** | **上限**——低于 1 秒就从「读谱」退化成「反应」 |
| 1800 px/s | 0.80 s | 硬核，本作不用 |

> **所以 `speedMax = 1400` 不是拍脑袋的数**，是「1 秒反应下限 × 1440px 前视」倒推的。
> 将来若把玩家 X 挪到 300（前视 1620px），上限可提到 1600 px/s。

### 4.2 加速曲线

```
speed(t) = min(speedBase + speedGain × t, speedMax)
```

| 参数 | 默认值 | 依据 |
|---|---|---|
| `speedBase` | 600 px/s | 开局 2.4 秒反应时间，够看清第一屏 |
| `speedGain` | 12 px/s² | 从 600 加到 1400 需 **66.7 秒**，正好覆盖一局的大半 |
| `speedMax` | 1400 px/s | 见 4.1 |

一局 90 秒的总距离：

```
600×66.7 + ½×12×66.7² + 1400×23.3 ≈ 40000 + 26700 + 32600 ≈ 99300 px
```

**取 1 米 = 100 px** → 一局 ≈ **993 米**，资产默认 `targetDistance = 1000 米`。
数字好看，且「米」这个单位玩家一眼就懂。

### 4.3 换道与最小安全间距

| 参数 | 默认值 |
|---|---|
| 车道 Y 间距 `laneGap` | 180 px（三车道共占 360 px 高） |
| 换道时长 `laneChangeTime` | 0.14 s |
| 障碍视觉宽度 | ≈ 120 px（= 1 格） |

最坏情况（速度 1400）下，从决定换道到完全离开原车道，横向前进
`1400 × 0.14 = 196 px`。加上障碍自身宽度 120 px：

```
最小安全间距 = 196 + 120 ≈ 316 px  →  取安全系数 1.5  →  480 px = 4 格
```

**写进 chunk 拼接器：两个 chunk 之间强制插入 ≥ 4 格空白。**

### 4.4 网格

| 量 | 值 | 说明 |
|---|---|---|
| 格宽 `cellWidth` | 120 px | 一屏 1920 px = **16 格** |
| chunk 长度 | 8 ~ 24 格 | 一屏能装下 1~2 个 chunk |
| 同屏物件上限 | 16 × 3 = **48** | 对象池按 64 金币 + 32 障碍预分配 |

### 4.5 判定宽容度

- 玩家判定盒 = 视觉尺寸 × **0.6**（跑酷惯例，擦身而过不算撞）
- 滑铲时判定盒高度 = 站立高度 × **0.45**
- 金币拾取盒 = 视觉尺寸 × **1.3**（吃金币要慷慨）
- 受击无敌 1.2 s，10 Hz 闪烁

### 4.6 ★ 跳跃与滑铲：必须按「距离」定，不能按「时间」定

这是加入跳跃后浮出来的**一个反直觉但结论明确的结果**，默认参数因此改过。

设跳跃跨越距离 `D`、玩家宽度 80 px、障碍宽度 120 px，则玩家必须在障碍前的
一段窗口内起跳，**容错窗口 = D − 200 px**。

**方案 A：固定滞空时间**（物理直觉的做法，`T_air` 恒定 0.5 s）

| 速度 | 跨越距离 D | 容错窗口(px) | 容错窗口(秒) |
|---|---|---|---|
| 600 | 300 | 100 | **0.17 s** |
| 1400 | 700 | 500 | **0.36 s** |

→ **速度越快，跳跃反而越容易**。这是错的：开局最慢的时候最难跳，
玩家刚上手就被卡住，越玩越轻松，难度曲线整个反了。

**方案 B：固定跨越距离**（`D` 恒定，滞空时间 = D / speed）

| 速度 | 滞空时间 | 容错窗口(px) | 容错窗口(秒) |
|---|---|---|---|
| 600 | 0.80 s | 280 | **0.47 s** |
| 1400 | 0.34 s | 280 | **0.20 s** |

→ 容错窗口随速度**单调收窄**，难度曲线方向正确。
代价是「跑得快时滞空更短」不符合真实物理——但跑酷本来就不真实
（Canabalt 也是速度越快跳得越远），且视觉上跳跃**高度恒定 120 px**、
只是抛物线播放得更快，看起来是「跑得快所以一跃而过」，反而更爽。

**结论：采用方案 B。**

| 参数 | 默认值 | 依据 |
|---|---|---|
| `jumpDistance` | **480 px（4 格）** | 容错窗口 0.20 s @1400 ≈ 12 帧，是能稳定按出来的下限 |
| `jumpHeight` | **120 px** | 必须 < `laneGap` 180，否则视觉上跨进邻道（见 1.3） |
| `slideDistance` | **360 px（3 格）** | 滑铲比跳跃短，鼓励用跳应对不确定情况 |
| `jumpMode` | `FixedDistance` | Def 里保留 `FixedTime` 选项，方便对比验证上表 |

**由此得出 chunk 校验的两条新规则**（写进 `VNRunnerTrack.Validate()`）：

1. 矮障碍（跳）之后 **4 格**内，同车道不得出现全高障碍——空中不能换道，落地即死
2. 高障碍（滑）之后 **3 格**内，同上

---

## 五、技术方案

### 5.1 坐标系：世界不动，物件横移 ★核心

玩家 X **固定**，不移动相机、不移动容器。每帧：

```csharp
foreach (var e in _active) e.x += dir * speed * dt;   // 出屏 → 回池
_distance += speed * dt;                              // 距离 = 速度对时间的积分
```

**不用 Physics2D**（与羽毛球一致）：碰撞是纯数学 AABB。
物理引擎在这里唯一的作用是引入不确定性和调试难度。

### 5.2 碰撞：统一走连续 Y 的 AABB，不按 lane index

**这是个容易踩的坑**：静态障碍待在车道上，按 `lane == playerLane` 判定很省事——
但**移动敌人是跨车道的**，玩家换道途中在两条车道之间，**跳跃时又在车道之上**。

若混用两套判定（静态按 index、其余按 Y），会出现「敌人视觉上明明在中间车道，
玩家在上车道却被判撞到」「明明跳过去了还是撞」的鬼畜。

**统一按连续 Y 做 AABB**，静态障碍的 Y 只是恰好等于车道中心而已。一套代码，零歧义。

三类障碍的差别只体现在**碰撞盒的高度与 Y 偏移**上，判定代码完全相同：

| 障碍 | 碰撞盒 | 躲法 |
|---|---|---|
| 矮障碍 `_` | 贴地，高 60 px | 跳（跳起后玩家盒底 > 60）|
| 高障碍 `"` | 悬空，从 70 px 到 180 px | 滑（滑铲后玩家盒顶 < 70）|
| 全高障碍 `#` | 0 ~ 180 px 全占 | 只能换道 |

> 跳跃和滑铲能不能过，**不是特判出来的，是 AABB 自然算出来的**。
> 这是「统一判定」最大的好处：不需要写 `if (isJumping && obstacle.type == Low)`。

另外碰撞按「上一帧 X → 这一帧 X」的**扫掠区间**判定，不是单点——
否则掉帧时一帧移动 200px 会直接穿过 120px 宽的障碍。

### 5.3 金币动画：`VNSpriteFlipbook`，明确不用 Animator

**Animator 在本项目是错误答案，三条硬理由：**

1. **破坏模块铁律 2**。项目模块一律 `VNTime.Delta`，为的是既躲开 Skip 快进、
   又**被 `VNPause` 冻住**（教程弹窗要能冻结玩法）。Animator 只有
   `Normal`（吃 `Time.timeScale`，Skip 时会变速）和 `UnscaledTime`
   （躲开暂停，教程弹出来金币还在转）两种更新模式，**没有一种对得上**。
2. **模块 UI 是运行时程序化生成的**。Animator 需要 AnimatorController 资产 +
   每枚金币一个 Animator 组件。同屏 48 个物件 = 48 个 Animator，
   对象池回收时还要 `Rebind()`，管理成本与开销都比自己写高。
3. **项目零 Animator 使用**（`grep` 只命中自写的同名类 `VNEntranceAnimator`）。
   引入它等于开一条新技术栈支线，与「全程序化、零美术依赖」的项目基调冲突。

**实现（~40 行）：**

```csharp
public class VNSpriteFlipbook : MonoBehaviour
{
    public Sprite[] frames; public float fps = 12f; public float phase;
    Image _img; float _t;
    void Update() {
        if (VNPause.IsPaused) return;          // 铁律 2：教程冻结时停住
        _t += VNTime.Delta;
        int i = Mathf.FloorToInt((_t + phase) * fps) % frames.Length;
        _img.sprite = frames[i];
    }
}
```

**同屏几十枚金币的优化**：把索引计算提到模块层，一帧算一次，
按 `phase` 分成 4 组错相广播（顺便让金币不整齐划一地闪，更自然）。
48 个物件从 48 次取模降到 4 次。

同一个组件将来直接给玩家的跑步 / 跳跃 / 滑铲逐帧动画用，不用二次开发。

### 5.4 课程生成：字符网格 chunk

```
# 3 行 = 上 / 中 / 下车道，每列 = 1 格（120px）
# .空  o金币  =整列金币  _矮障碍(跳)  "高障碍(滑)  #全高障碍(换道)  ^上下巡逻敌人
..o.._....o.....
.oo..._..#..^.o.
..o..."......^..
```

**两种模式共存**（决策 2）：

| 模式 | 行为 |
|---|---|
| `random`（默认） | 按权重随机抽 chunk 无限拼接，接缝插 ≥4 格空白，跑够 `targetDistance` 结束 |
| `fixed` | 按 Def 里写死的 chunk 序列顺序播放，播完即终点（剧情关 / 教程关，可复现） |

chunk 资产带 `minSpeed / maxSpeed` 字段，随机模式下只在速度区间内抽——
这样「高难 chunk」会随游戏进程自动出现，**难度曲线不用另写一套**。
也用它把「需要跳」「需要滑」的 chunk 压到教学之后才出现。

### 5.5 背景滚动：模块自绘，走 RawImage 的 uvRect

**不能复用 `VNBackgroundScroll`**——那是舞台组件，模块碰它违反铁律 1。

模块内自铺 2~3 层 `RawImage`（远景 / 中景 / 地面），每帧只改 `uvRect.x`，
纹理 `wrapMode = Repeat`。一层一个 Image，不用拼两张图，
视差速度按层设 0.2 / 0.5 / 1.0 倍。

> 技术选择与 `VNBackgroundScroll` 一致（滚 UV 不拼图），
> 但那边是 Image + 自定义 shader（要处理图集 UV 的 `_UVRect` 坑），
> 模块这边用 RawImage 更省——RawImage 直接吃 Texture，没有图集问题。

### 5.6 层级与射线

- 模块实例化在 EventLayer，Canvas 排序 **60**（ChoicePanel 45 之上、转场 100 之下）
- 模块自绘的一切 `raycastTarget = false`（唯 ESC 确认框例外）——同 aitalk 的教训
- 输入走新版 Input System：`Keyboard.current.wKey / sKey / spaceKey / leftShiftKey`

### 5.7 暂停与生命周期

```csharp
void Update() {
    if (VNPause.IsPaused) return;   // ★ 必须在读输入之前
    float dt = VNTime.Delta;        // 含 0.05s 单帧上限，防掉帧穿模
    ...
}
```

- 所有 Tween `SetLink(gameObject)` + `SetUpdate(true)`
- ESC 弹确认框放弃 → 判「失败」
- `CancelForDebug()` 清理对象池与协程

### 5.8 地面影子：不是装饰，是核心可读性部件

见 1.3。每个角色（玩家 + 敌人）在自己车道线上有一枚椭圆影子：

- 换道时，影子跟着一起平移
- 跳跃时，**影子留在原车道线上**，按高度缩小、变淡
- 落地时影子瞬间摊开一下（`VNFootShadow.Impact()` 的思路，但模块自绘，不碰舞台组件）

**这是玩家判断「我在哪条车道」的唯一权威信号**，不能因为「看起来像装饰」而砍掉。

### 5.9 输入缓冲：决策 11 的必要补偿

空中与滑铲中不能换道（决策 11），但玩家在空中一定会按 W/S。
若直接吞掉输入，高速下是很挫败的「按了没反应」。

```csharp
// 落地前 0.15 秒内按下的换道请求，落地那一帧立即执行
if (laneInput != 0) { _bufferedLane = laneInput; _bufferTimer = 0.15f; }
...
if (grounded && _bufferTimer > 0f) { ChangeLane(_bufferedLane); _bufferTimer = 0f; }
```

同样地，**跳跃也要有起跳缓冲**：落地前 0.12 秒按下的 Space，落地瞬间起跳
（连续跳过两个相邻矮障碍时必需，否则玩家必须精确等到落地帧）。

### 5.10 音效（代码合成，同羽毛球 / 擦雾）

`VNRunnerSfx`：金币（上行短音，连吃时音高递增做 combo 反馈）、起跳、落地、
滑铲（摩擦噪声）、撞击（低频噪声爆）、加速里程碑、结算。

---

## 六、文件清单

### 6.1 新增 —— 运行时（`Assets/Project/Scripts/VNEffects/Script/`）

| 文件 | 预估行数 | 职责 |
|---|---|---|
| `VNRunnerDef.cs` | ~280 | 资产定义：速度 / 加速 / 跳滑 / 生成 / 判定 / 命数 / chunk 库 / 金币帧 |
| `VNRunnerChunk.cs` | ~130 | 单个区块资产：字符网格 + 权重 + 速度区间 + 解析成物件列表 |
| `VNRunnerTrack.cs` | ~320 | **纯静态，无 MonoBehaviour，可单测**：chunk 拼接、接缝安全间距、难度曲线、扫掠 AABB、**跳滑落点可解性校验**（4.6 两条规则） |
| `VNRunnerScore.cs` | ~100 | **纯静态判定数学**：三档结果、成绩换算（结构照抄 `VNFogScore` / `VNTouchScore`） |
| `VNRunnerActor.cs` | ~280 | **表现层集中地**：玩家 / 敌人 / 障碍外观、换道缓动、跳跃抛物线、滑铲姿态、**地面影子**、受击闪烁。换真素材只改这一个文件 |
| `VNSpriteFlipbook.cs` | ~50 | 通用逐帧动画组件（金币 / 将来的角色动画共用，**可复用给其他模块**） |
| `VNRunnerModule.cs` | ~720 | 事件模块：UI 搭建、主循环、输入与缓冲、HUD、进度条、命数、结算、flag 回写 |
| `VNRunnerSfx.cs` | ~170 | 七个代码合成音效 |

> **分层理由**：`VNRunnerTrack` / `VNRunnerScore` 不继承 MonoBehaviour，
> 因为手感全靠调这里的数，而调数需要能脱离场景反复跑。
> 沿用 `VNBadmintonBallistics` / `VNFogMask` / `VNTouchScore` 的一贯习惯。

### 6.2 新增 —— 编辑器（`Assets/Project/Scripts/VNEffects/Editor/`）

| 文件 | 预估行数 | 职责 |
|---|---|---|
| `VNRunnerInstaller.cs` | ~140 | 场景装机：Tools → VN Effects → 场景装机 → **跑酷 Runner Module**（照抄 `VNQuizInstaller`） |
| `VNRunnerChunkEditor.cs` | ~240 | chunk 的**可视化网格编辑器**：点格子切换六种内容，**实时跑 4.6 的可解性校验并标红** |
| `VNRunnerTuneWindow.cs` | ~240 | 调参窗口：输入参数 → **算出**预计通关秒数、各时刻反应时间、**各速度下的跳跃容错窗口**（4.6 那张表实时重算） |

> `VNRunnerChunkEditor` 不是加分项。手写 3×24 的字符网格在 Inspector 里
> 对不齐、数不清列号，而且加了跳滑之后**肉眼根本看不出「跳完落地会不会撞墙」**——
> 校验必须由工具做。

### 6.3 新增 —— 资产与内容

| 路径 | 内容 |
|---|---|
| `Assets/Art/VNEffects/Runner/*.asset` | 至少一个示例 `VNRunnerDef` + 10~14 个内置 chunk（分三批：只换道 / 加跳 / 加滑） |
| `Assets/Scenarios/RunnerDemo.vn.txt` | 演示剧本（随机模式 + 固定关卡各一段） |
| `Assets/Art/VNEffects/Runner/Tutorial.asset` | `VNTutorialDef` 新手引导（三语，四步：换道 / 跳 / 滑 / 命数） |

### 6.4 改动清单

| 文件 | 改动 |
|---|---|
| `VNGameConfig.cs` | 新增 `public List<VNRunnerDef> runners`（玩法页） |
| `VNGameConfigEditor.cs` | 把 `runners` 登记进「玩法」页签 |
| `VNProceduralTextures.cs` | 新增占位金币 / 三类障碍 / 敌人 / 跑道 / 影子贴图 |
| `VNScenarioSchema.cs` | `EventVariants["runner"]` 补一行参数定义（**存储键必须是 `module` 不是 `id`**） |
| `VNScenarioLinter.cs` | 检查 `id:` 是否已登记、`* 失败` 是否被接住 |
| `VNTutorialAnchors` 调用 | 模块内 `Register("runner.lives", rect)` 等，供教程高亮 |
| `VNEventRegistry`（场景） | 装机器自动登记模板，无需手改代码 |

### 6.5 明确**不**改动

- `VNSaveData` / `RestoreSnapshot` / `RebuildStateBefore` —— **模块状态不进存档**
  （event 期间本就不能存档，跑酷随模块销毁，没有「读档后该不该还在」的问题）
- `VNBackgroundScroll` / `VNStage` / `VNFootShadow` / 舞台演出的一切 —— 铁律 1，
  模块自铺背景与影子
- `VNCamera` / 运镜系统 —— 跑酷不动镜头，靠物件横移

---

## 七、资产字段设计

### 7.1 `VNRunnerDef`

```
[标识]
  runnerId            string    剧本 event runner id:<这里> 引用
  title/titleEn/titleJa         HUD 标题（三语）

[赛制]
  goalMode            enum      Distance（跑够距离）| Time（撑够时长）
  targetDistance      float     1000（米）
  targetTime          float     90（秒，goalMode=Time 时用）
  lives               int       3
  courseMode          enum      Random（chunk 随机拼接）| Fixed（固定序列）
  runDirection        enum      Left（默认）| Right

[速度与难度]
  speedBase           px/s      600
  speedGain           px/s²     12
  speedMax            px/s      1400      ← 别超 1600，见 4.1
  playerScreenX       px        480       ← 前视距离 = 1920 - 这个数

[跑道]
  laneCount           int       3（暂固定，架构上允许 5）
  laneGap             px        180
  laneCenterY         px        跑道中心在屏幕的 Y
  laneChangeTime      s         0.14
  cellWidth           px        120

[跳跃与滑铲]                                 ← 见 4.6，按距离定不按时间定
  jumpMode            enum      FixedDistance（默认）| FixedTime
  jumpDistance        px        480（4 格）
  jumpHeight          px        120       ← 必须 < laneGap，否则视觉跨道
  jumpAirTime         s         0.5       （仅 FixedTime 模式用）
  slideDistance       px        360（3 格）
  slideHitboxScale    0~1       0.45      滑铲时判定盒高度
  laneInputBuffer     s         0.15      空中按 W/S 的缓冲窗口
  jumpInputBuffer     s         0.12      落地前按 Space 的缓冲窗口
  canChangeLaneInAir  bool      false     ← 决策 11

[生成]
  chunks              List<VNRunnerChunk>   随机模式的 chunk 库（带权重）
  fixedCourse         List<VNRunnerChunk>   固定模式的顺序序列
  minGapCells         int       4           接缝最小空白格数
  coinDensityScale    0~2       1.0         全局金币密度倍率
  hazardDensityScale  0~2       1.0         全局障碍密度倍率

[敌人]
  enemyAmplitude      lane      1.0         上下巡逻跨几条车道
  enemyPeriod         s         1.6
  enemyPhaseRandom    bool      true        相位随机（否则一排敌人同步摆动很假）

[判定]
  playerHitboxScale   0~1       0.6
  coinPickupScale     1~2       1.3
  invulnTime          s         1.2
  blinkHz             float     10

[三档判定]
  perfectRule                   固定为「全程不掉血」（决策 7，不做成字段）

[外观]
  coinFrames          List<Sprite>   金币逐帧（留空 = 程序化金币）
  coinFps             float          12
  bgLayers            List<Texture>  2~3 层视差背景（留空 = 程序化渐变）
  bgParallax          List<float>    每层速度倍率
  shadowAlpha         0~1            0.35   地面影子浓度

[音效]
  coinSe / jumpSe / landSe / slideSe / hitSe / milestoneSe   留空 = 代码合成
```

### 7.2 `VNRunnerChunk`

```
  chunkId        string
  grid           string[3]   三行字符网格（. o = _ " # ^）
  weight         float       随机模式的抽取权重（默认 1）
  minSpeed       px/s        低于此速度不抽（简单 chunk 设 0）
  maxSpeed       px/s        高于此速度不抽（0 = 无上限）
  note           string      作者备注（如「S 型走位」「连续两跳」）
```

---

## 八、剧本语法设计

### 8.1 典型用法

```
# 迟到的早晨：跑够 1000 米就能赶上早自习
bg 上学路
show 星野结衣 pos:right
event runner id:上学路 dist:1000 stat:体力 rate:0.05 flag:跑酷1
* 完美
    结衣　「你今天…怎么一次都没撞到人？」
* 通关
    结衣　「呼，赶上了。」
* 失败
    旁白　迟到了。教导主任正在校门口等着你。
```

```
# 剧情关：固定关卡，可复现，首次自动播教程
event runner id:教学关 mode:fixed tutorial:跑酷入门
* 通关
* 失败
```

### 8.2 参数表

| 参数 | 必填 | 说明 |
|---|---|---|
| `id:` | ✅ | `VNRunnerDef` 资产 id（在 `VNGameConfig.runners` 登记） |
| `dist:` | | 目标距离（米），覆盖资产默认 |
| `time:` | | 目标时长（秒），`goalMode=Time` 时用 |
| `mode:` | | `random` / `fixed`，覆盖资产默认 |
| `life:` | | 命数，覆盖资产默认 3 |
| `speed:` | | 起始速度倍率（0.8 = 简单，1.2 = 难） |
| `stat:` / `rate:` | | 成绩换算成属性加成，接口同 `event photo` / `event wipefog` |
| `flag:` | | 成绩 flag 前缀 |
| `tutorial:` | | 覆盖模块默认教程 id（基类已支持） |

### 8.3 结果名（精确字符串匹配，永不翻译）

```
完美 / 通关 / 失败
```

> `* 失败` **建议接住**：玩家手残时会静默跳过整段剧情。Lint 给警告而非错误。

### 8.4 回写的 flag

| flag | 内容 |
|---|---|
| `<前缀>_距离` | 跑出的米数（整数） |
| `<前缀>_金币` | 本局吃到的金币数 |
| `<前缀>_剩余命` | 结束时剩几条命 |
| `<前缀>_最长无伤` | 最长连续无伤距离（米） |
| `<前缀>_档位` | 完美 / 通关 / 失败 |
| `事件结果` | 按项目惯例由 Runner 写入 |

> 想让任务系统写出「单局跑满 1500 米」这类条件，需要先在
> `VNGameConfig.trackers` 登记 `<前缀>_距离` 的统计声明，派生出 `@最高`。

---

## 九、分阶段实施计划

### P1 核心循环可跑（不接剧本）

- [ ] `VNRunnerTrack`：chunk 解析、拼接、扫掠 AABB、可解性校验（纯静态）
- [ ] `VNRunnerModule` 骨架：铺跑道、玩家占位、物件横移、对象池
- [ ] W/S 换道 + 缓动；Space 跳（抛物线）；Shift 滑
- [ ] 输入缓冲（5.9）
- [ ] 一个临时测试入口，直接 Play 就能跑
- **验收**：三类障碍各自只能用对应动作躲过去；空中按 W 落地后会执行；
  60 FPS 不掉帧，出屏物件正确回池

### P2 表现与手感

- [ ] `VNRunnerActor`：占位外观、**地面影子**（5.8）、受击闪烁、金币吸入动画
- [ ] `VNSpriteFlipbook` + 金币逐帧（有素材就接，没有走程序化）
- [ ] 移动敌人（上下巡逻，相位随机）
- [ ] 视差背景（RawImage uvRect）
- [ ] `VNRunnerSfx` 七个音效
- **验收**：**能一眼看出自己在哪条车道**（跳跃时也是）；能看出速度在涨；
  撞击有反馈；敌人不同步摆动

### P3 资产化与调参

- [ ] `VNRunnerDef` / `VNRunnerChunk` 全字段
- [ ] `VNRunnerChunkEditor` 可视化网格编辑器 + 可解性标红
- [ ] `VNRunnerTuneWindow` 调参窗口（通关秒数、反应时间、跳跃容错窗口表）
- [ ] 手写 10~14 个内置 chunk（只换道 / 加跳 / 加滑 三批）
- **验收**：不改代码就能调出「简单 / 普通 / 困难」三种手感；
  故意写一个「跳完撞墙」的 chunk 会被工具标红

### P4 接剧本与系统联动

- [ ] `event runner` 接入：kwargs、三档结果、flag 回写
- [ ] `VNScenarioSchema` / `VNScenarioLinter` 登记
- [ ] `stat:` / `rate:` 成绩换算
- [ ] HUD：进度条、命数、金币数、速度表、按键提示
- [ ] ESC 确认框、`CancelForDebug`
- **验收**：`RunnerDemo.vn.txt` 三条分支都能走通，Lint 零报错

### P5 装机、教程与文档

- [ ] `VNRunnerInstaller` 增量装机菜单
- [ ] `VNTutorialDef` 四步新手引导 + `VNTutorialAnchors.Register`
- [ ] 三语文案
- [ ] 文档：`WhatAiDo.md` 新章节 + `CLAUDE.md` 组件表 + `HowToUse.md` 语法 + `ProjectCodeGuide.md`
- **验收**：空场景跑一次装机器就能玩；教程弹出时跑酷真的冻住

---

## 十、风险清单与保险丝

| 风险 | 保险丝 |
|---|---|
| **跳跃与换道视觉混淆**（1.3 的轴冲突） | 四重区分：位移量 120<180 / 抛物线 / 时长差 / **地面影子**。影子是硬需求不是装饰，P2 验收项直接考它 |
| **跳完落地撞全高障碍**（无解） | 4.6 的两条校验规则写进 `VNRunnerTrack.Validate()`，`VNRunnerChunkEditor` 实时标红 |
| **生成无解墙** | chunk 手写保证内部可解；接缝强制 ≥4 格空白；校验器扫「三车道同时被堵」 |
| **难度曲线跑反**（速度越快越简单） | 4.6 已证明「固定滞空时间」会导致这个结果，故采用固定距离制；调参窗口实时显示各速度容错窗口 |
| **空中按键被吞** | 5.9 输入缓冲，0.15 s 换道 + 0.12 s 起跳。缺了它决策 11 就是 bug |
| **速度上限调过头**，退化成纯反应游戏 | 调参窗口显示各时刻反应时间，低于 1.0 s 标黄、低于 0.8 s 标红 |
| **教程冻不住模块** | `Update()` 首行 `if (VNPause.IsPaused) return;`，且必须在读输入之前——晚一行，玩家在教程期间照样能跳 |
| **对象池泄漏** | `CancelForDebug` / `OnDestroy` 都清池；Tween 全部 `SetLink` |
| **掉帧穿模** | `VNTime.Delta` 自带 0.05 s 单帧上限（收编自羽毛球）；碰撞按扫掠区间判定 |
| **金币 sheet 被导入设置改坏** | `VNTextureImportDefaults` 只在 `importSettingsMissing` 时设，手动改成 Multiple 后不会被打回 |

---

## 十一、被否决的方案与理由

| 方案 | 否决理由 |
|---|---|
| **Animator 播金币动画** | 见 5.3 三条硬理由。核心是 Animator 的两种更新模式都对不上项目「躲快进但受暂停」的语义 |
| **跳跃用固定滞空时间** | 见 4.6。会让「速度越快跳跃越容易」，难度曲线整个反向 |
| **Physics2D 做碰撞** | 引入不确定性、调试难度，且 FixedUpdate 与 `VNTime.Delta` 的时间基准冲突。羽毛球已经证明纯数学判定更可控 |
| **移动相机 / 移动容器** | 跑酷距离会持续增长，容器 X 无限增大终会撞上浮点精度。业界标准就是物件横移 + 回池 |
| **复用 `VNBackgroundScroll` / `VNFootShadow`** | 都是舞台组件，模块碰它们违反铁律 1；且模块要多层视差、影子要跟跳跃联动 |
| **纯参数随机撒点** | 手感是「均匀的随机」，读谱乐趣归零，且可解性要在运行时做图搜索。已被决策 2 排除 |
| **按 lane index 判碰撞** | 移动敌人跨车道、玩家换道途中在车道之间、跳跃时在车道之上，多套判定必然打架。见 5.2 |
| **三类障碍写成特判** (`if isJumping && type==Low`) | 统一 AABB 天然算得出来，特判只会在「跳到一半换道」「滑铲遇到移动敌人」这类边角情况漏掉分支 |
| **改成伪 3D 往画面深处跑** | 那是 Subway Surfers，不是你要的横版侧视，工作量也大幅增加 |
| **拾取道具 / 养成属性联动** | 决策 9 排除。架构里留了扩展点（`PickupType` 枚举位），随时可加 |

---

## 十二、工时估算

| 阶段 | 估算 |
|---|---|
| P1 核心循环 | 中等偏大（加了跳滑与输入缓冲，状态机比原方案复杂一档） |
| P2 表现与手感 | 中等（影子系统是新增的必做项） |
| P3 资产化与调参 | 中等（网格编辑器 + 可解性校验占大头） |
| P4 接剧本与联动 | 小（有 4 个模块的现成范式可抄） |
| P5 装机教程文档 | 小（教程从两步变四步） |

> 总量略大于「擦雾」、接近「羽毛球」。
> **内容量**（手写 chunk）比两者都大，且是长期持续投入的部分——
> 所以 P3 的网格编辑器与可解性校验不能省。
