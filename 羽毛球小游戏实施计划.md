# 羽毛球小游戏实施计划

> **状态**：方案已定案（11 项决策全部拍板），P1 开工中
> **撰写日期**：2026-08-11
> **参考来源**：`D:\_A QuickStart\Unity & Game\Unity Source Code Sample\Student Age new\MiniGame.Badminton\`
> **目标**：VN 剧本流程中可以 `event badminton` 拉起一局完整的羽毛球对战，胜负回流剧情分支
> **分支**：`agent/badminton-minigame`

---

## 目录

- [一、可行性结论](#一可行性结论)
- [二、参考实现拆解（玩法配方）](#二参考实现拆解玩法配方)
- [三、已定案的十一项决策](#三已定案的十一项决策)
- [四、坐标与参数换算表](#四坐标与参数换算表)
- [五、文件清单](#五文件清单)
- [六、分阶段实施计划](#六分阶段实施计划)
- [七、剧本语法设计](#七剧本语法设计)
- [八、美术需求清单（需要你提供）](#八美术需求清单需要你提供)
- [九、风险清单与保险丝](#九风险清单与保险丝)
- [十、工时估算](#十工时估算)
- [十一、被否决的方案与理由](#十一被否决的方案与理由)

---

## 一、可行性结论

**这次不是「搬运」，是「照着逻辑重写」。**

与 `卡牌游戏并入视觉小说专案.md` 那次合并的性质完全不同：

| | 卡牌那次 | 羽球这次 |
|---|---|---|
| 来源 | 完整 Unity 工程（含资产/预制体/asmdef） | **只有反编译 .cs 源码** |
| 工作重心 | 把独立 App 改造成宿主的子系统 | 把玩法配方在本专案范式内重新实现 |
| 主要风险 | 相机堆叠 / timeScale / 巢状 Canvas | 手感参数换算 / 帧率下的击球窗口穿透 |
| 美术 | 直接搬 | 全部自研（程序化 + 你提供两张立绘） |

我确认过来源目录：326 个 `.cs`，非代码文件只有 `.vs/` 与 `obj/` 的编译缓存——**没有任何美术、预制体、DragonBones 骨骼数据**。

好消息是**配方完整**：`BadmintonMiniGameView.cs`（1263 行）把弹道数学、AI 决策、精准判定、赛制全部写死在一个文件里，已逐行读完，可以 1:1 复刻逻辑层。

---

## 二、参考实现拆解（玩法配方）

### 2.1 文件构成

| 文件 | 行数 | 作用 |
|---|---|---|
| `BadmintonMiniGameView.cs` | 1263 | 全部玩法逻辑 + UI 事件 + 结算 |
| `BadmintonPlayer.cs` / `BadmintonEnermy.cs` | 各 47 | 仅负责异步加载 DragonBones 预制体、取 Animator 引用 |
| `State.cs` | 15 | 十态枚举 `None/Init/Start/Play/Score/Net/Wait/Win/Lose/Pause` |

### 2.2 弹道：解析式抛物线，不是物理模拟 ★核心

`CalcTrack(起点, 落点, 力度, 是否扣杀)`：

1. 先求「球网处（x=0）的过网高度」：以起点→落点连线在 x=0 处的高度为下限，在 `[netMinY, netMaxY]` 之间随机取一个；**扣杀时直接取下限 +10**（贴网而过）。
2. 用**起点、落点、过网点三点定二次曲线**，解出 `y = ax² + bx + c`（源码里那三坨行列式展开就是在解这个）。
3. 飞行速度 `flySpeed = sqrt(|g · 200 / (2a)|) · direction`——曲率越平的球飞越快、高吊球慢。扣杀且 `flySpeed > 1800` 时再乘力度系数。
4. 每帧 `x += flySpeed · dt`，`y` 直接代公式；球体旋转角跟随位移方向。

**为什么这样做而不是物理**：抛物线一旦定下来，落点、最高点、对手该跑到哪里**全部可以立刻解析求出**，AI 不需要预测、轨迹虚点不需要模拟。整个玩法的可控性都建立在这上面。

### 2.3 轨迹虚点 = 难度旋钮

沿抛物线每 `deltaTrackX` 生成一个对象池点，球飞过就回收。`trackDisplayRate` 控制「预告显示多长的一截」：新手关给你看全程，高难度只给一小段。截图 1、3 里那串白点就是它。

### 2.4 击球判定：按键 ≠ 击球

- 按击球键 → 只是**触发挥拍动画**（球高 > `hit2Y` 播 `Hit` 高手位，否则 `Hit2` 低手位）；
- 真正判定发生在**球与球拍 trigger 的碰撞帧**；
- `CalcAccurate(球x − 人x, 是否扣杀)`：
  - `−20 ~ +perfectDistance` → 返回 `1.0`，**必定界内**，冒「精准」字（截图 3）
  - `> perfectDistance` → 返回 `perfectRate`（扣杀且 ≤160 仍算 1.0，扣杀有宽容窗口）
  - 球在身后（< −20）→ 返回 `0.5`
- 落点 `GetRandomEndPos(accurate)`：按该概率决定落界内，否则落到界外 `+50 ~ +300`。

**设计要点**：打不准不是立刻失误，而是**概率性出界**。这让手感不挫败，也让「精准」有明确的正反馈。

### 2.5 扣杀

跳跃键起跳（只有来球最高点 > `jumpNeedY` 才值得跳）→ 空中击球 `Hit3` → `isHeavy = true` → 过网高度压到最低、速度乘力度。玩家的 `power / jumpHigh / moveSpeed` 三项从技能等级读：

```csharp
playerPower     = SportConstCfg[601] + skillLvl * SportConstCfg[602];
playerJumpHigh  = SportConstCfg[605] + skillLvl * SportConstCfg[606];
playerMoveSpeed = SportConstCfg[603] + skillLvl * SportConstCfg[604];
```

**这就是养成联动的全部**——一条 `base + 等级 × 每级增量` 的线性式。

### 2.6 AI：先知式跑位，不是实时追球

`CalcTrack` 算出球路的**同时**就把对手的击球点 `hitPos` 和该跑到的 `targetEnermyX` 一起解出来了。对手只是朝那个 x 匀速跑，到点了按 `hitPos.y` 决定跳/高手/低手。

三个难度旋钮：

| 参数 | 含义 |
|---|---|
| `enermyHitRate` | 到位了也有概率漏球（`Random.value > hitRate` 直接不还击） |
| `enermyHeavyRate` | 有多爱扣杀（且只在来球够高时） |
| `enermyMoveSpeed` | 跑位速度 |

配上 `perfectRate`（AI 界内率）、`trackDisplayRate`（给玩家看多少轨迹）、`power`（扣杀力度），一共 6 项组成 `BadmintonLevelCfg`。

### 2.7 计分与赛制

- 球 `y ≤ minY + 10` → 进 `Score` 态；
- 判 `|x| > moveArea.max + 50` 是否出界 → 出界则**击球方失分**，界内则**接球方失分**；
- 球网碰撞单独走 `Net` 态；
- **5 分制 + 净胜 2 分（deuce）**，`playerScore >= maxScore && playerScore - enermyScore >= 2`；
- 得分后球从 kinematic 转 dynamic，让它自然翻滚落地（纯观感）。

### 2.8 台词气泡

`LoveBadmintonCfg` 六类随机台词：`faqiu`（发球）/ `hit`（击球）/ `heavyHit`（扣杀）/ `opHit`（对手打得好）/ `score`（得分）/ `loseScore`（失分）。每次 50% 概率触发、同一侧气泡未消失不叠加。截图 2 那句「哈哈，又得一分」就是 `score`。

### 2.9 结算铁律

**胜负只设标志位，回流一律写在 `CloseView()` 一条路径**——通关、失败、中途退出全部走同一条。这条你的 `ReferenceProjectLearnings.md §43(b)` 已经记下来了，这次落地。

---

## 三、已定案的十一项决策

| # | 议题 | 决策 |
|---|---|---|
| 1 | 角色动画 | **程序化假动画**：静态底图 + DOTween 旋转/蹲起/位移/残影，接口留好可换真动画 |
| 2 | 玩法还原度 | **完整还原**：移动 + 起跳扣杀 + 轨迹预告 + 精准判定 + AI 三率 + 5 分制净胜 2 分 |
| 3 | 属性联动 | 扣杀力度 / 跳跃高度 / 移动速度从 VN 养成属性 flag 读（照 `VNBattleModule.patkstat` 范式） |
| 4 | 比分回写 | 结束写 flag（我方得分 / 对方得分 / 精准数 / 最长回合），剧本可做「虽败犹荣」细分支 |
| 5 | 难度配置 | `VNBadmintonDef` ScriptableObject，多对手多难度，登记进 `VNGameConfig` |
| 6 | 台词气泡 | 做，六类随机台词配在 Def 资产里（三语） |
| 7 | 场景美术 | **程序化画**：球场线/网/地面/横幅/记分板全用 uGUI 生成，背景复用现有 `Art/Images/Background` |
| 8 | 键位 | 键盘 `A/D`（+`←/→`）移动，`J` 发球兼击球，`K` 起跳扣杀；**鼠标只负责击球**（左键击球 / 右键扣杀），移动仅键盘 |
| 9 | 中途退出 | `ESC` 弹确认框，确认即**判负**，走「失败」结果行（结算单一路径） |
| 10 | 配套 | 演示剧本 ✅ / 自由练习模式 ✅ / 音效（代码合成先顶上）✅ / **Editor 调参窗口 ❌** |
| 11 | 角色底图 | **你另外提供侧身运动立绘**（规格见第八节），P1/P2 先用占位图搭 |

### 关于第 10 项没做调参窗口的补偿

你没勾 Editor 预览窗口，意味着调手感只能进 Play Mode。补偿措施：

> **模块在 `Application.isEditor` 下每帧重读 `VNBadmintonDef` 的手感参数**，Play 着的时候直接拖 Inspector 就能实时看到变化，不用反复进出 Play Mode。运行时构建下这段用 `#if UNITY_EDITOR` 完全编译掉，零开销。

---

## 四、坐标与参数换算表

### 4.1 推断出的参考设计分辨率

参考实现用 `anchoredPosition`，锚点 `(0.5, 0)`（底边中心）。从数值反推：

- x 半宽用到 `moveArea.max 1000` + 出界带 300 → 半宽至少 1300
- y 用到 1200（抛物线顶点上限）
- `minY 260` 是地面线

对照截图（角色脚底约在画面 18% 高处、球网上沿约 40%），**参考设计分辨率约 2560×1440**。本专案 Canvas 是 **1920×1080**，正好是 **0.75 倍**——所有长度量一律乘 `k = 0.75`。

### 4.2 换算表

| 参考值 | 含义 | 本专案（×0.75） |
|---|---|---|
| `minY = 260` | 地面线（落地判定 / 角色站立 y） | **195** |
| `ballStartY = 500` | 发球时球的初始高度 | **375** |
| `ballStartX = 400` | 发球目标落点 x（距中线） | **300** |
| `playerStartX = 500` | 双方起始站位 x | **375** |
| `netMinY = 600` | 过网最低高度（网上沿） | **450** |
| `netMaxY = 1000` | 过网最高高度（高吊上限） | **750** |
| `hit2Y = 650` | 低手/高手挥拍的球高分界 | **488** |
| `jumpNeedY = 800` | 值得起跳扣杀的来球高度 | **600** |
| `moveArea = (200, 1000)` | 可移动范围（距中线） | **(150, 750)** |
| 出界带 `max+50 ~ max+300` | 界外落点抽样区间 | **+38 ~ +225** |
| `perfectDistance = 120` | 精准判定的球拍距离窗口 | **90** |
| `deltaTrackX = 50` | 轨迹虚点间距 | **38** |
| 抛物线顶点上限 `1200` | 钳制 | **900** |
| 跳跃 `米 → px` 的 `×100` | `jumpHigh` 单位换算 | **×75** |

### 4.3 ★ flySpeed 必须额外修正（否则手感偏躁）

`flySpeed = sqrt(|g · 200 / (2a)|)`。

全部坐标乘 `k` 后 `a → a/k`，于是 `flySpeed → √k · flySpeed`。但要保持**飞行时间不变**需要的是 `k · flySpeed`。

- 需要：`0.75 ×`
- 实际得到：`√0.75 = 0.866 ×`

**因此换算后必须再乘 `√k = 0.866`**，否则球相对屏幕会快约 15%，回合明显变短、来不及跑位。这个系数在 Def 资产里暴露为 `flySpeedScale`，默认 `0.866`。

> 这是本次移植最容易踩的静默坑：数值全部照抄、看起来也能跑，但手感就是不对，而且很难归因。

---

## 五、文件清单

### 5.1 新增 —— 运行时（`Assets/Project/Scripts/VNEffects/Script/`）

| 文件 | 职责 | 预估行数 |
|---|---|---|
| `VNBadmintonBallistics.cs` | **纯静态数学**：三点定抛物线、飞行速度、落点抽样、精准判定、AI 击球点求解。无 MonoBehaviour / 无 UnityEngine UI 依赖，可单测 | ~200 |
| `VNBadmintonModule.cs` | `VNEventModule` 子类：状态机、主循环、输入、计分、赛制、结算 | ~600 |
| `VNBadmintonActor.cs` | 角色表现层：六态程序化假动画 + 球拍 + 脚影。**对外只暴露状态接口**，将来换真动画只改这里 | ~280 |
| `VNBadmintonCourt.cs` | 程序化球场：地面 / 场地线 / 球网 / 广告横幅 / 记分板 / 提示横幅 | ~260 |
| `VNBadmintonDef.cs` | 对手 + 难度 + 台词 + 手感参数 + 音效槽位 ScriptableObject | ~200 |
| `VNBadmintonSfx.cs` | 5 个音效的代码合成（`AudioClip.Create`），Def 槽位填了就覆盖 | ~120 |
| `VNBadmintonUi.cs` | 三个文件共用的程序化 UI 辅助 + `VNBadmintonQuad`（uGUI 画不出梯形，球场透视地面靠它）。**施工中新增，原计划没有** | ~120 |

### 5.2 新增 —— 编辑器（`Assets/Project/Scripts/VNEffects/Editor/`）

| 文件 | 职责 |
|---|---|
| `VNBadmintonInstaller.cs` | `Tools → VN Effects → 场景装机 Install To Scene → 羽毛球对战 Badminton Module`：增量装机（照 `VNQuizInstaller` 范式，**模板必须带 RectTransform**），不重建场景、可重复执行 |

### 5.3 新增 —— 资产与内容

| 路径 | 内容 |
|---|---|
| `Assets/VNEffects/Badminton/` | 示例 `VNBadmintonDef` ×3（新手 / 校队 / 王牌） |
| `Assets/Scenarios/BadmintonDemo.vn.txt` | 演示剧本：对话 → 开打 → 胜负分支 → 后续对话 |

### 5.4 改动清单

| 文件 | 改动 |
|---|---|
| `VNGameConfig.cs` | 加 `public List<VNBadmintonDef> badmintons`（玩法区，紧邻 `quizzes`） |
| `VNEffectsDemoSetup.cs` | 扫 `Assets/VNEffects/Badminton` 自动登记 + 场景模板生成 |
| `Editor/VNScenarioLinter.cs` | ① outcome 白名单 `["badminton"] = {胜利, 失败, 结束}`；② 新增 `unknown-badminton` 检查（照 `unknown-quiz`） |
| `Editor/VNScenarioSchema.cs` | `event` 已是通用命令，无需改结构；补参数提示文案 |
| `VNLocale` UI 字符串表 | 新增 `badminton.*` 键（三语） |
| `CLAUDE.md` | 组件速查表加 3 行 + 剧本子系统表加 1 行 |
| `WhatAiDo.md` | 新章节（详细记录） |
| `HowToUse.md` | `event badminton` 语法与参数表 |
| `ProjectCodeGuide.md` | 第六节玩法扩展层补条目 |

### 5.5 明确**不**改动

- **不碰 `VNScriptParser` / `VNScriptRunner`** —— `event` 是既有通用命令，加小游戏不需要动 DSL 主干（这是事件模块契约的价值所在）。
- **不碰 `VNSaveData`** —— 事件模块状态不进存档是既有契约；与剧情通信只走 flags。
- **不引入 Physics2D** —— 见第十一节。

---

## 六、分阶段实施计划

### P1 核心逻辑可跑 ✅ 已完成（2026-08-11）

**目标**：进 Play Mode 能看到球场、能发球、球沿抛物线飞、有轨迹虚点、能击球、能得分、打到目标分结束。角色用占位色块，无难度资产，无音效。

- [x] `VNBadmintonBallistics.cs`：三点定抛物线 / flySpeed（含 `√k` 修正）/ 落点抽样 / 精准判定 / 接球点反解
- [x] `VNBadmintonUi.cs`：共用 UI 辅助 + 梯形 Graphic
- [x] `VNBadmintonCourt.cs`：程序化球场（天空/横幅/沙地/透视球场/场地线/球网/记分板/提示横幅/操作提示）
- [x] `VNBadmintonActor.cs`：角色表现层骨架（占位剪影 + 球拍旋转 + 脚影 + 呼吸/前倾），接口已按「换真动画只改这一个文件」设计
- [x] `VNBadmintonModule.cs`：五态状态机 + 主循环 + 球飞行 + 轨迹点对象池 + 触网/落地判定 + 计分 + 净胜 2 分 + flag 回写
- [x] **击球窗口子步进**（见风险 ①）
- [x] 键盘输入（A/D、←/→ + J + K）与鼠标击球（左键击球 / 右键扣杀）
- [x] AI 接球（先知式跑位 + 按「还有多久到位」决定起拍/起跳，比参考实现的距离阈值更稳、帧率无关）
- [x] Play Mode 实测：自动对拉打到 2-2、双方都能得分，说明发球→飞行→接球→精准判定→落点→计分全链路通

**P1 实测踩到的坑（已修，记在这里免得再犯）**

1. **`Graphic` 子类必须自己再写一遍 `[RequireComponent(typeof(CanvasRenderer))]`**——基类上那条不会被 `AddComponent` 走继承链读到。少了它的症状极具迷惑性：组件 active、enabled、material、canvas、顶点数据**全部正常**，就是不画。稳妥做法是创建时显式 `AddComponent<CanvasRenderer>()`。
2. 构建顺序有依赖：`ResetPositions()` 会碰 `_hitMarker`，必须排在 `BuildBall()` 之后。
3. `ShowTips` 是**单条通道**，连喊两次会把前一条掐掉（赛点提醒要并进同一条文案，不能再喊一次）。
4. 球体不能只用 `SparkleSprite`——在亮底球场上几乎看不见。现在是「软光晕 + 实心球头 + 羽毛裙」三层。
5. 网面别用实心半透明矩形，会变成一块压住球场的灰盒子。改成极淡底 + 网格线。

**P1 额外产出**：`VNBadmintonModule.debugAutoPlayer` 开关——让玩家也交给 AI 自动对拉，用来验证回合逻辑（正式使用关掉）。P4 的自由练习模式演示也能复用。

### P2 角色表现层 ✅ 已完成（2026-08-11）

- [x] `VNBadmintonActor.cs` 重写：六态程序化假动画。挥拍用 `SwingDef` 数据表描述
      （时长 / 蓄力角 / 抽击角 / 身体前倾 / 击球压缩），三段插值 蓄力 0~0.28 →
      抽击 0.28~0.52（平方加速）→ 收拍 0.52~1
- [x] **手臂枢轴在肩膀**：转它 = 球拍绕肩画弧，即使没有手臂图也读得出「挥」。
      有 `arm` 图就画在同一枢轴下
- [x] 球拍独立图，拍柄末端为轴；占位球拍 = 拍杆 + `RoundedFrameSprite` 空心拍框
- [x] 扣杀残影（三张渐隐身体副本，压在本体后面）
- [x] 脚影随高度收缩变淡 + `Land()` 落地摊开与身体压缩（离地→着地自动侦测，模块不用管）
- [x] 占位剪影升级为 头 + 躯干 + 双腿（走动时双腿反相摆动）
- [x] 台词气泡（宽度跟台词长度走）
- [ ] 你的侧身立绘到位后替换占位图 ← **等素材**

### P3 AI 与难度资产 ✅ 已完成（2026-08-11）

- [x] `VNBadmintonDef.cs`：对手 + 难度 + 台词 + 立绘 + 赛制合并成一个资产
      （对应参考实现的 `BadmintonLevelCfg` + `LoveBadmintonCfg` 两张表）
- [x] 3 个示例资产：`新手` / `校队` / `王牌`（`Assets/VNEffects/Badminton/`），已登记进 `VNGameConfig.badmintons`
- [x] Def 解析三级：剧本 `id:` → 剧本 `vs:`（同名资产）→ 库里只有一条时直接用
- [x] 立绘三级回退：Def 羽球专用图 → Def/`vs:` 指定角色的 `VNCharacterDef` 默认立绘 → 模板兜底图
- [x] AI 先知式跑位 + 三率（`opponentHitRate` / `opponentHeavyRate` / `opponentMoveSpeed`）
- [x] AI 起拍提前量改为**直接问表现层要「拍面几时开始有效」**（`ActiveWindowStart`），动画时长改了自动跟上
- [x] `trackDisplayRate` 接进难度（新手 1.0 全程 / 校队 0.55 / 王牌 0.22）
- [x] 台词气泡：六类随机（发球/击球/扣杀/夸对手/得分/失分），同侧不叠加，
      概率走 Def 的 `talkRate`；得分失分强制必说（rate 1），平球降到 0.25 免得一直刷屏
- [x] **Editor 下每帧重读 Def 手感参数**（决策 10 不做调参窗口的补偿）：Play 着直接拖
      Def 的 Inspector 就能实时看效果。用 `JsonUtility` 往返就地覆盖，加字段不用改这里；
      `#if UNITY_EDITOR` 包住，运行时构建整段编译掉

**三档难度参数**

| | 扣杀倾向 | 接球率 | 轨迹预告 | 界内率 | 移速 | 力度 | 目标分 |
|---|---|---|---|---|---|---|---|
| 新手 | 0.05 | 0.62 | 1.00 | 0.85 | 5.0 | 0.45 | 5 |
| 校队 | 0.25 | 0.88 | 0.55 | 0.72 | 8.0 | 0.75 | 5 |
| 王牌 | 0.45 | 0.96 | 0.22 | 0.60 | 10.5 | 1.00 | 7 |

**P2/P3 实测踩到的坑**

6. **Game View 截图会是旧帧**。编辑器窗口不在前台时 Game View 不重绘，`screenshot-game-view`
   会返回上一次的画面——我因此追了三次「明明建好了却看不见」的幽灵。
   **验证渲染一律用 `screenshot-camera`**（直接渲到 RenderTexture，必定新帧）。
7. **编辑器后台时 Play Mode 只跑 ~0.5 FPS**（`Time.unscaledDeltaTime` 高达 2.16s）。
   模块自己有 `dt` 上限 0.05 所以玩法不会乱，但 **DOTween 没有这个钳制**——
   一帧就把整段 1.7s 的气泡序列跑完了，截图当然什么都截不到。
   要看瞬时演出就手动 `Kill()` tween 再把状态定住。
8. 台词气泡宽度必须按 `GetPreferredValues` 算，固定宽度会被长句撑破。

### P4 系统联动与音效 ✅ 已完成（2026-08-11）

- [x] `powerstat: / speedstat: / jumpstat:` 读 flag：能力 = Def 基础值 +
      `Clamp(属性值, 0, statCap)` × 每点增量（增量与上限配在 Def）
- [x] `ApplyTuning()` 统一入口：`CopyFrom(def.tuning)` **之后**再叠加成，
      顺序反了会被编辑器每帧重读冲掉
- [x] 比分/精准数/最长回合写回 flag（抽成 `WriteFlags()`，正式赛与自由练习共用）
- [x] `ESC` 确认框 → 判负。确认框开着时整局冻结（球不动、输入只走确认框），
      支持鼠标点按钮与 Y/N/Enter/Esc；**认输与打输走同一条 `Finish(false)`**（照 §43b）
- [x] `mode:free` 自由练习：不判胜负、不喊赛点、ESC 不弹框直接收工，结果名「结束」，
      右下角提示与开场横幅换成练习版文案
- [x] `VNBadmintonSfx.cs`：五个音效代码合成（指数衰减包络 ×（衰减正弦 + 低通白噪），
      配比 `noise` = 这一拍有多闷），Def 五个槽位填了就逐条覆盖，
      音量跟随 `VNAudio.seVolume`

### P5 装机、内容与文档 ✅ 已完成（2026-08-11）

- [x] `Editor/VNBadmintonInstaller.cs` 增量装机（Tools → VN Effects →
      **Install Badminton Module To Scene**），照 `VNQuizInstaller` 范式，可重复执行
- [x] `VNEffectsDemoSetup` 生成场景时建 `BadmintonTemplate` + 登记 id `badminton`
- [x] `VNGameConfigTools` 两条路径都补上羽球对手库（从场景导入 / 扫资产目录）
- [x] `Assets/Scenarios/BadmintonDemo.vn.txt`（自由练习 → 属性联动正式赛 →
      比分 flag 细分支 → 换难度重赛）
- [x] Linter 两项：结果名白名单 `badminton` + `unknown-badminton` 对手 id 检查
- [x] `VNLocale` 三语 UI 字符串共 17 个 key
- [x] 文档：WhatAiDo 一〇二章 / CLAUDE 组件表与子系统表 / HowToUse
      「羽毛球对战：badminton」小节 + 事件模块总表 + Lint 表 + 速查卡 /
      ProjectCodeGuide「示例模块⑤」
- [ ] 合并回 `main` ← 等用户验收后执行（分支永不删除）

**P4/P5 踩到的坑**

9. **装机器结尾的 `EditorUtility.DisplayDialog` 会卡住 MCP**：模态框阻塞 Unity 主线程，
   自动化调用会一直超时直到有人手点 OK。用脚本跑安装器时要有心理准备
   （或者给安装器加一个 `silent` 参数走无对话框路径）。

---

## 七、剧本语法设计

### 7.1 典型用法

```
# 剧情赛：跟小雪打一场校队水平的比赛
角色 小雪 「今天就分个高下吧。」
event badminton vs:小雪 id:校队 target:5 first:me powerstat:体育 jumpstat:体育
* 胜利 -> 赢了小雪
* 失败 -> 输给小雪
```

```
# 自由练习：放学后随便打两下，没有胜负
event badminton vs:小雪 mode:free
* 结束
```

```
# 虽败犹荣分支：靠比分 flag 细分
event badminton vs:王牌 id:王牌 target:5
* 胜利 -> 爆冷
* 失败
if 羽球_对方得分 - 羽球_我方得分 <= 1 jump 惜败
jump 惨败
```

### 7.2 参数表

| 参数 | 默认 | 说明 |
|---|---|---|
| `vs:` | 必填 | 对手角色 id（`VNCharacterDef.id`），用于名字 / 头像 / 立绘回退 |
| `id:` | `vs` 同名 | `VNBadmintonDef` 资产 id（难度 + 台词 + 手感）；找不到用内置默认难度 |
| `target:` | `5` | 目标分数（净胜 2 分制不变） |
| `first:` | `random` | `me` / `opponent` / `random` 谁先发球 |
| `mode:` | `match` | `match` 正式赛 / `free` 自由练习（无胜负、可随时退出） |
| `powerstat:` | 无 | 扣杀力度读哪个属性 flag（不填用 Def 基础值） |
| `speedstat:` | 无 | 移动速度读哪个属性 flag |
| `jumpstat:` | 无 | 跳跃高度读哪个属性 flag |
| `flag:` | `羽球` | 比分回写的 flag 前缀 |
| `title:` | Def 标题 | 覆盖记分板标题 |

### 7.3 结果名（精确字符串匹配，永不翻译）

| 结果名 | 触发 |
|---|---|
| `胜利` | 达到目标分且净胜 2 分 |
| `失败` | 对手达标，或 `ESC` 确认退出 |
| `结束` | 仅 `mode:free` |

### 7.4 回写的 flag

| flag | 内容 |
|---|---|
| `<前缀>_我方得分` | 最终比分 |
| `<前缀>_对方得分` | 最终比分 |
| `<前缀>_精准数` | 本局打出「精准」的次数（可做「打得漂亮」分支） |
| `<前缀>_最长回合` | 最长一回合的来回次数（可做「拉锯战」分支） |

---

## 八、美术需求清单（需要你提供）

程序化假动画需要每个角色**一张侧身底图 + 一张球拍图**。规格：

### 8.1 角色侧身立绘

| 项 | 要求 |
|---|---|
| 数量 | 至少 2 张（玩家 1 + 对手 1）；每多一个对手多 1 张 |
| 朝向 | **一律面向右侧**——左侧角色我用 `flipX` 翻转，你只需要画一个朝向 |
| 姿态 | 正侧面或侧面偏 3/4 的**中立站姿**，全身含脚，膝盖微屈 |
| 持拍手 | 右手（靠镜头侧）自然半举；**球拍不要画进立绘**（球拍单独一张，我要独立旋转它） |
| 尺寸 | ≥ 512 × 1024 PNG 透明背景 |
| 对齐 | **脚底贴齐图片底边**（我按底边对齐球场地面线） |
| 留白 | 左右各留约 10% 透明边，避免挥拍旋转时被裁 |
| 最终显示 | 在 1920×1080 中高度约 340px（约占屏高 1/3，与参考截图一致） |

### 8.2 球拍单图

| 项 | 要求 |
|---|---|
| 尺寸 | ≥ 256 × 256 PNG 透明背景 |
| 构图 | 拍面朝右上，**拍柄末端在图片左下角**——我以左下角为旋转轴做挥拍 |

### 8.3 可选（有会好很多）

- 一张「手臂前伸挥拍瞬间」的替换手臂图 → 扣杀动作会明显更有力
- 球场背景大图（若不给，走程序化 + 复用现有 `Art/Images/Background` 里的校园图）

> **在你给图之前**：P1/P2 用程序化剪影占位，玩法完全可玩，换图不改任何逻辑代码。

---

## 九、风险清单与保险丝

### ① ★ 帧率导致击球窗口穿透（最高风险）

球按 `x += flySpeed · dt` 推进。扣杀球 `flySpeed` 极大（参考里 >1800 px/s，我们换算后 >1350 px/s），30fps 下单帧位移 45px，而精准窗口只有 90px——**低帧率时球会直接跳过球拍**。

参考实现靠 `Rigidbody2D` 的连续碰撞检测规避了这个；我们改纯数学判定后**必须自己补**。

> **保险丝**：主循环做**子步进（substep）**——把每帧位移按最大步长（默认 12px）切成若干小步，逐步推进并检测击球窗口与落地线。步数上限 16 防死循环。

### ② 快进 / Skip 模式

模块内一律 `Time.unscaledDeltaTime`，所有 Tween `.SetUpdate(true)`——事件模块三铁律第 2 条。

### ③ Tween 泄漏

模块随时可能被销毁（`CancelForDebug` / 玩家退出），所有 Tween `.SetLink(gameObject)`——三铁律第 3 条。

### ④ 一局 3~5 分钟不可存档

既有契约，不改。缓解：`ESC` 确认框上明写「退出将判负」，避免玩家误以为能存档。

### ⑤ 新输入系统的 null 检查

`Keyboard.current` / `Mouse.current` 在无设备时为 `null`，每次访问前判空（照 `VNQteModule` 写法）。

### ⑥ 模板必须带 `RectTransform`

装机器创建模板物体时 `new GameObject(name, typeof(RectTransform))`——`VNQuizInstaller` 里那条 `★` 注释踩过的坑，模块 `BuildUi` 直接 `(RectTransform)transform` 会抛 `InvalidCastException`。

### ⑦ 正面立绘打羽球会奇怪

已通过决策 11 规避（你提供侧身图）。占位期间用剪影，不会误导手感评估。

### ⑧ Editor 每帧重读 Def 的开销

用 `#if UNITY_EDITOR` 包起来，运行时构建完全编译掉。

---

## 十、工时估算

| 阶段 | 内容 | 估时 |
|---|---|---|
| P0 | 分支 + 本文档 | 0.5h（已完成） |
| P1 | 核心逻辑 + 程序化球场 + 占位角色 | 1 天 |
| P2 | 角色表现层（程序化假动画） | 0.5~1 天 |
| P3 | AI + 难度资产 + 台词气泡 | 0.5 天 |
| P4 | 属性联动 + flag 回写 + ESC + 自由模式 + 音效 | 0.5 天 |
| P5 | 装机器 + 演示剧本 + Lint + 本地化 + 文档 | 0.5 天 |
| | **合计** | **约 3~4 天** |

不含你提供侧身立绘的时间；立绘到位后替换占位图约 0.5h。

---

## 十一、被否决的方案与理由

| 方案 | 否决理由 |
|---|---|
| **照抄 Physics2D + Collider2D 做击球判定** | 要在 Canvas 里塞 `Rigidbody2D`/`Collider2D`、配 `Player`/`Enermy` Tag 与 Layer、还要管 `Physics2D.simulationMode`——为一个小游戏污染全局物理配置。改纯数学判定后：确定性可重放、零全局配置、无 Tag/Layer 依赖。代价是要自己补子步进（风险 ①）。 |
| **独立场景 additive 加载** | 注册表的场景模式尚未实现（`WhatAiDo` 四十四章评审结论），且引入相机堆叠风险。uGUI 单场景完全够用。 |
| **引入 DragonBones / Spine 运行时** | 为一个小游戏加一整套骨骼动画运行时 + 资产管线 + 美术工作流，收益不成比例；而且参考项目的骨骼数据根本拿不到。 |
| **做成新 DSL 命令 `badminton` 而非 event 模块** | 会绕开事件层的输入接管、回想记录、结果行契约、调试清理，破坏既有一致性。`event` 契约的存在意义就是「加玩法不动 DSL 主干」。 |
| **第一版就做 Editor 调参窗口** | 你的决策（第 10 项）。用「Editor 下每帧重读 Def」作为低成本补偿。 |

---

## 附：与 `ReferenceProjectLearnings.md` 的对应

本次落地了参考清单里的两条：

- **§43(b) 结算一律写在 `CloseView()`** → 决策 9：胜/负/退出走单一结算路径
- **§44 40 个小游戏可抄清单** 里的「羽球」 → 本文件

尚未落地、但本模块预留了接口的：

- **§43(a) `MiniGameFromType` 来源维度** → 目前只支持从剧本 `event` 进入。将来若要从日程/地图进入，在 `VNEventContext` 上加一个 `from` 维度即可，模块侧不用改。

---

## 十三、验收后的调整（2026-08-11 晚）

用户装入真实素材（立绘 / 球拍 / 羽毛球图）后实测，反馈「说是五分却打到七八分还没完」。

### 根因：不是 bug，是自由练习模式

用户从演示剧本第 10 行开始播，第一段是 `mode:free`，设计上就没有终局。
证据：右下角提示是「ESC 结束练习」而非「ESC 认输」。

**但这是真缺陷**——玩家无从区分「练习不会结束」与「正式赛坏了不判胜负」，
开场横幅一闪而过就没了。修法是给记分板加**常驻副标题**：

- 正式赛：`先到 N 分 · 需净胜 2 分`
- 自由练习：`自由练习 · 不计胜负 · ESC 结束`

副标题会与右侧对手名挤在一起，顺带把记分板改成双行布局（名字与比分同行、
副标题独占下行，高度 120 → 148）。

### 同批修掉的三个真 bug

1. **确认框开着时 J/K 仍能挥拍起跳**——`_confirmOpen` 的拦截写在 `ReadInput()` 之后，
   「冻结整局」名不副实。挪到前面。
2. **首帧球停在原点**——`OnLaunch` 末尾没做视觉同步。实测 `球 逻辑位置=(300,375)
   画面位置=(0,0)`。60fps 下看不出，低帧率会在左下角闪一下。补 `SyncVisuals()`。
3. **脚影几乎看不见**——软光晕边缘衰减快，0.32 的黑压在绿场上等于没有，
   而落地感全靠它。加深到 0.5、尺寸略收。

### 查证后确认**不是** bug 的两件事

- **两个角色其实是镜像的**：运行时读到 `我方 pivotScaleX=1.00 / 对手 pivotScaleX=-1.00`。
  看不出来是因为素材是正面站姿且左右近似对称。
- **三个对手长得一样**：素材装在模块模板的兜底槽位而非各自 Def
  （`def「新手」body=False racket=False ball=False`），走兜底路径共用一张图。
  用户确认「现在这样就行」，不改。

### 手感调整：球路抬高

用户反馈「球来球往的高度有点低」。新增 `netClearance`（普通球至少高出网顶多少，
默认 120）作为「回合整体高度」的旋钮，同时 `netMaxY` 750 → 880、`apexClamp` 900 → 1000。

200 次抽样实测：

| | 平均顶点高度 | 平均球速 |
|---|---|---|
| 抬高前 | 610 px | 657 px/s |
| 抬高后 | **732 px（+20%）** | **564 px/s（−14%）** |

球更高**且自动变慢**——速度由曲率反解，弧越弯越慢。不用另外调速度。

> 三个 Def 资产序列化的是旧值，改代码默认值不会自动生效，必须写进资产（已写）。
> 新增的 `netClearance` 因为不在旧序列化数据里，Unity 会保留 C# 字段初始值 120。

### 通用改动：事件期间隐藏常驻养成 HUD

用户反馈顶部属性条（金钱/行动力/压力/善恶）盖在小游戏上面。
在 `VNScriptRunner` 的 event 流程里加 `SetGameplayHudVisible(false/true)`，
**对 battle / quiz / shop / plan 等所有事件模块一并生效**。
退出事件时恢复成「玩家是否手动隐藏了 UI」的状态（读 `_uiHidden`），
不会把手动隐藏的又翻出来。

### 用户明确不改的

- 素材位置（保持装在模板兜底槽，三个对手共用一张脸）
- 自由练习保持无限对拉、只能 ESC 退
