# AI 自由聊天（aitalk）完全指南

> 这份文档讲**原理**和**怎么调**。只想知道语法照着写的，看 `HowToUse.md` 六章的
> 「AI 自由聊天：aitalk」小节就够了。
>
> 相关文件：人格资产在 `Assets/Art/VNEffects/AiPersonas/`，
> 代码在 `Assets/Project/Scripts/VNEffects/Script/VNAi*.cs`，
> 演示剧本 `Assets/Scenarios/AiTalkDemo.vn.txt`，
> 对话日志写在项目根的 `AiTalkLogs/`。

---

## 一、一分钟上手

```
show 星野结衣 at:left
event aitalk vs:星野结衣 turns:8 topic:恋爱话题 place:放学后的空教室 stat:好感 rate:2 flag:闲聊_
* 好感提升 -> 聊得不错
* 普通
* 冷场 -> 气氛尴尬
* 失败 -> 连不上
```

跑之前要做两件事（各一次）：

1. **装模块**：Tools → VN Effects → 场景装机 Install To Scene → AI 自由聊天 AI Talk Module，然后 Ctrl+S 存场景
2. **配 Key**：按当前用的那家配（用 Tools → VN Effects → AI → 查看 Key 状态 Show Key Status 确认读到了）
   - DeepSeek（**默认**）：环境变量 `DEEPSEEK_API_KEY`，或项目根放 `DeepSeekAiApiKey.txt`
   - Gemini：环境变量 `GEMINI_API_KEY`，或项目根放 `GeminiAiApiKey.txt`

> 换供应商 = 改 `VNGameConfig` 的「AI 默认供应商」一处，详见 **十七、两家供应商**。

---

## 二、一轮对话到底发生了什么

这是理解全部调参的基础。**每一轮**都完整走一遍这七步：

```
① 组装 system prompt  ←── 人格资产 + 剧本参数 + 当前 flag 值
② 组装 contents       ←── 最近 N 轮的完整对话历史
③ 组装 responseSchema ←── 表情/漫符 enum 从角色资产实时生成
   ↓ 一次 HTTPS 请求（约 1.5 秒）
④ 收到一个 JSON       ──→ 台词 / 表情 / 漫符 / 好感变化 / 三个选项 / 是否收尾
⑤ 校验与钳制          ──→ 好感强制 Clamp、表情越界降级、选项不足补齐
⑥ 演出                ──→ 换表情、打字机、漫符、停 0.4s、三个选项飞入
⑦ 玩家点一个          ──→ 那句话成为下一轮 contents 的最后一条
```

### 关键认知：API 是无状态的

两家的服务器都**不记得**你上一轮说了什么。所谓「她记得你三轮前提过猫」，
实际是我们**每一轮都把整段对话重新发过去一遍**。

这解释了几乎所有性价比问题：

| 现象 | 原因 |
|---|---|
| 越聊到后面越贵 | 历史越长，每轮的输入 token 越多 |
| 聊久了她会「忘记」开头 | 历史被 `historyTurns` 裁掉了，不是她「忘了」，是根本没发过去 |
| 改人格资产立刻生效 | system prompt 每轮重建，不缓存 |

实测（同一场对话的前三轮输入 token）：

```
第 1 轮  923 tok  ←── system prompt 约 900 + 开场提示
第 2 轮  943 tok  ←── 多了第 1 轮的一问一答
第 3 轮  973 tok  ←── 又多了一轮
```

每轮约 +25~40 token，一轮的**边际成本**大概是 $0.0007，21 轮约 $0.015（一毛钱）。

---

## 三、记忆：三层，只有两层是通的

| 层 | 范围 | 谁控制 | 现状 |
|---|---|---|---|
| **短期** | 本轮请求发出去的历史 | 人格资产 `historyTurns` | ✅ |
| **中期** | 本场对话的累计结果 | `flag:` 前缀写的那几个 flag | ✅ |
| **长期** | 跨场记忆（下次她还记得） | 人格资产 `enableMemory` / `memoryCapacity` | ✅ |

### 短期记忆 = `historyTurns`

发给模型的历史只保留最近 N 轮。裁掉的部分**不做摘要，直接丢弃**。

```
historyTurns = 12   →   第 13 轮时，第 1 轮的内容已经不在请求里了
```

**这是最容易配错的一个参数。** 剧本写 `turns:21` 但人格资产 `historyTurns` 还是 12 的话，
聊到后半段她会答非所问——因为她真的看不到前面聊了什么。

规则很简单：**`turns:` 不要超过 `historyTurns`**，超了就把 `historyTurns` 一起调上去。
对话日志会在文件头自动帮你检查这一条：

```
- **轮数上限**：21　**历史保留**：12 轮
  > ⚠ 轮数上限(21) > 历史保留(12)：第 13 轮起，最早的对话会被裁掉，她会「忘记」开头。
```

代价是钱：`historyTurns` 翻倍，后半程每轮的输入 token 也大致翻倍。
12 轮的对话用 `historyTurns=12` 全程不裁，是个合理的默认。

### 中期记忆 = flag

一场聊完，模块把结果写进 flag，**剧本能读到**（详见第七节）。
这是「AI 聊天」和「手写剧情」之间唯一的正式接口。

### 长期记忆 = 跨场记忆

**一场聊完，模块会额外发一次请求做总结**，把这一场沉淀成三样东西：

| 产物 | 去哪 | 干什么用 |
|---|---|---|
| `summary` 一句话 | 记忆（存档） | 下次注入，给关系的连续性 |
| `topics` 话题标签 | 记忆（存档） | **下次作为「不要重复」的硬清单注入** |
| `facts` 具体信息 | 记忆（存档） | 让她「记得」你答应过什么 |
| `diary` 日记正文 | 日记本（全局） | 给玩家看，见第十四节 |

下次 `event aitalk` 时，system prompt 会多出两段：

```
【你还记得的事】
你和「我」之前已经聊过 3 次，按时间从早到晚：
  1.（放学后的空教室）两人讨论了生活态度，气氛轻松愉快。
  2. ...
你记得的具体事情：
  - 他叮嘱我要注意身体

【避免重复】
下面这些话题**之前已经聊过了，这次不要再当作新话题提起**：
  人生哲学、上课睡觉、自拍、身体健康
要开新话题。如果非要提到旧话题，必须是「延续」而不是「重新问一遍」——
比如上次聊过值日，这次可以说「上次那袋垃圾后来怎么样了」，
但不能再问一次「今天轮到谁值日」。
```

> **为什么话题清单要单独成段、措辞强硬？**
> 实测把话题揉进摘要里再说「别重复」效果差得多。让模型**逐个避开一份明确的清单**，
> 比让它读一段散文然后自己判断有效得多。这是「少重复」这个需求的主力机制。

**存档语义**：记忆**跟着存档走**（`VNSaveData.aiMemories`）。读回第 3 章的档，
她就不会提起第 5 章聊过的东西——因为对话记忆是剧情状态，不是玩家的收藏品。
（日记本相反，走全局，见第十四节。）

**调参**：

| 字段 | 作用 |
|---|---|
| `enableMemory` | 关掉 = 每次见面都是全新的她（也不写日记） |
| `memoryCapacity` | 最多记住最近几场，默认 15。调大 = 记性好但每轮输入 token 更多 |

**成本**：每场结束多一次请求，约 $0.001。相对一场 $0.006~0.013 可以忽略。
总结请求固定用 `thinking: Minimal`（不跟随人格设置），免得人格开了 `High`
之后每场结束都白白多花几千个思考 token。

### 手写记忆

`memory:` 参数仍然可用，内容和自动记忆**叠加**而不是互相覆盖：

```
event aitalk vs:星野结衣 memory:她昨天在走廊上哭过，但没说原因
```

适合把剧本里发生的事塞给她——自动记忆只覆盖 AI 聊天，不知道主线剧情。

---

## 四、输入：system prompt 是怎么拼出来的

打开任意一份对话日志，翻到最后的「附：首轮 system prompt」，你会看到完整的一份。
它由六段拼成，**顺序是有讲究的——越靠后的指令，模型实际权重越高**：

| # | 段落 | 内容来自 | 每轮变化？ |
|---|---|---|---|
| 1 | 身份与性格 | 人格资产 `persona` | 否 |
| 2 | 说话方式 | 人格资产 `speechStyle` | 否 |
| 3 | 你和「我」的关系 | 人格资产 `relationship` | 否 |
| 4 | **此刻的情况** | 剧本参数 + 当前 flag 值 | **是** |
| 5 | 输出规则 | 代码固定 + 角色资产的表情表 | 否 |
| 6 | **绝对边界** | 人格资产 `boundaries` | 否 |

第 4 段是唯一每轮都在变的：

```
场景：放学后的空教室            ←── 剧本 place:
当前关系：好感 40，走得比较近的同学  ←── 剧本 stat: 读 flag，再翻译成人话
这次想聊的：恋爱话题             ←── 剧本 topic:（只在第 1 轮给）
这场对话大约还剩 18 轮，请自然地把话题引向收尾  ←── 每轮递减
```

> **为什么好感要翻译成人话？**
> 直接写「好感=40」模型只会复读数字。写成「走得比较近的同学」它才会调整语气分寸。
> 阈值写在 `VNAiTalkModule.BuildAffectionText()`，五档：
> `<20 还不太熟` / `<40 说得上话但有距离` / `<60 走得比较近` / `<80 关系很好` / `≥80 心照不宣`。

---

## 五、输出：AI 必须返回的六个字段

模型必须返回这六项，一个都不能少。
**Gemini 是 JSON Schema 服务端强制；DeepSeek 只能靠提示词约束**（软的，见第十七节），
所以下面「越界了怎么办」那一列在 DeepSeek 上会真的用上。

| 字段 | 类型 | 驱动什么 | 越界了怎么办 |
|---|---|---|---|
| `reply` | 字符串 | 对话框台词（打字机） | 太长只告警，不截断 |
| `emotion` | **枚举** | `VNStage.Say` 换立绘表情 | 降级到第一个表情 |
| `mark` | **枚举** | `VNCharacterMarks` 弹漫符 | 不出符号 |
| `affection_delta` | 整数 | 累计好感 → 结果判定 + 属性联动 | **强制 Clamp** |
| `options[3]` | 数组 | 三个候选回复（含隐藏语气标签） | 不足 3 个补「……」 |
| `should_end` | 布尔 | AI 自己判断话题聊完了 | — |

### 枚举是怎么来的（这是设计核心）

`emotion` 的可选值**不是写死的**，是运行时从角色资产读出来的：

```
VNCharacterDef.expressions → ["默认","微笑","害羞","惊讶","生气","沮丧","坏笑"]
                           ↓ 塞进 responseSchema 的 enum
                模型物理上不可能输出这七个之外的值
```

所以换个角色自动适配，加一个表情立刻可用，在 Gemini 上**永远不会出现「AI 编了个不存在的表情名」**。
（DeepSeek 没有 enum 硬约束，编得出来——所以才有下面那道代码校验，见第十七节。）

`mark` 同理，外加一个固定的 `none`（必须给它一个「这轮不出符号」的合法选项，
否则它每轮都会硬塞一个）。

### 为什么还要在代码里再校验一遍

**Schema 只约束形状，不约束取值范围。** Gemini 的 schema 子集不支持 `minimum`/`maximum`——
提示词里写了「-2 ~ +2」，实测它照样给过 **+5**。所以代码里强制 `Clamp` 并告警。

这条推广开来就是一句话：**永远不信任模型输出**。表情越界降级、漫符非法忽略、
选项不足补齐——宁可演出打折，也不让模块崩或数值失控。

---

## 六、调参手册：改哪里 → 什么效果 → 什么原理

### 人格资产（`Assets/Art/VNEffects/AiPersonas/*.asset`）

这是 90% 的调参入口。改完**立刻生效**，不用重启 Play Mode。

| 字段 | 症状 → 改它 | 原理 |
|---|---|---|
| `persona` | 说话不像这个角色 | 进 system prompt 第 1 段，定义她是谁 |
| `speechStyle` | 语气不对、太书面 | 第 2 段。**写具体的口头禅比写形容词有效十倍**——「爱用『真的假的？』」远胜「性格活泼」 |
| `relationship` | 她对主角的态度不对 | 第 3 段。三个候选回复的反应也由它决定 |
| `boundaries` | 她出戏 / 编剧情 / 人设崩 | **第 6 段，位置最靠后 = 权重最高**。想强制的规则写这里。**必须列清单，别只写抽象原则**——实测只写「不编造重大剧情（转学/告白结果/他人生死）」时，AI 自己抛出了怀孕剧情；改成正面约束「只聊日常」+ 明确清单 + 「拿不准就换轻松话题」之后才稳住 |
| `optionTones` / `useExtendedOptions` | 想要 3 条还是 5 条 | 见第八节。资产定默认，剧本 `options:N` 可覆盖 |
| `thinking` | 选项区分度不够 / 成本太高 | **最贵的开关**，见第十节。`High` 比 `Minimal` 贵约 6 倍、慢约 4 倍 |
| `maxReplyChars` | 台词太长，打字机拖 | 只是提示词里的一句话，不是硬截断。40~50 比较合适 |
| `affectionClamp` | 好感涨太快 | 提示词里说范围 + 代码强制 Clamp 双保险 |
| `historyTurns` | 聊久了她忘事 / 想省钱 | 见第三节。**和剧本的 `turns:` 配套调** |
| `optionTones` | 想换选择维度 | 见第八节。改这里等于改整个玩法的选择轴 |
| `temperature` | 回答太套路 / 太跳脱 | 0.9 稳、1.0~1.2 活泼、>1.3 开始失控。辣妹用 1.15 |
| `provider` | 想换供应商 | 「跟随全局」= 用 `VNGameConfig` 里设的那家。见第十七节 |
| `model` | 质量不够 / 太贵 | 留空 = 当前供应商的默认模型（DeepSeek `deepseek-v4-flash` / Gemini `gemini-3.5-flash-lite`） |
| `maxOutputTokens` | 日志出现「输出超长被截断」 | 512 够用；调大要连带看成本 |
| `safety` | 暧昧台词被拦 | `BlockOnlyHigh` 抬高阈值，**不是关掉审核**。**DeepSeek 没有这个参数**，调了没用 |
| `allowedEmotions` | AI 选了会穿帮的表情 | 留空 = 全部。只在素材构图不统一时才列白名单 |
| `fallbackLines` | 断网时说什么 | 断网/被拦/解析失败都用这个，随机挑一句 |

### 剧本参数（每次调用可不同）

| 参数 | 效果 |
|---|---|
| `vs:` | 谁跟你聊（角色 id）。省略 `persona:` 时按它自动找人格 |
| `persona:` | 用哪套人格。**一个角色可以有多套**（日常 / 约会 / 吵架后） |
| `turns:` | 这次最多聊几轮 |
| `topic:` | 聊什么（只在第 1 轮注入）|
| `place:` | 场景描述，影响她提到的环境细节 |
| `me:` | 玩家的称呼，影响候选回复的人称 |
| `stat:` `rate:` | 好感变化 × rate 加到属性；同时把当前值翻译成人话喂进去 |
| `flag:` | 成绩 flag 前缀 |
| `memory:` | 手写的「往事」，注入 system prompt |
| `options:` | 这次给几个候选回复（3~6）。不写 = 按人格资产的扩展开关 |

### 模块字段（场景里的 AiTalkTemplate）

| 字段 | 效果 |
|---|---|
| `optionDelay` | 打字机放完到选项飞入之间的停顿，默认 0.4s |
| `logMode` | 对话日志：仅编辑器（默认）/ 总是 / 关闭 |

---

## 七、flag 完全清单

设 `flag:闲聊_`，模块在结束时写入：

| flag | 内容 |
|---|---|
| `闲聊_轮数` | 实际聊了几轮（提前结束就是实际轮数） |
| `闲聊_好感变化` | 累计 `affection_delta` 之和 |
| `闲聊_倾向_温柔` | 玩家选了几次「温柔」 |
| `闲聊_倾向_玩笑` | 选了几次「玩笑」 |
| `闲聊_倾向_直球` | 选了几次「直球」 |

> 倾向 flag 的名字跟着 `optionTones` 走——改了语气标签，flag 名也跟着改。

另外 `stat:好感 rate:2` 会把 `好感` 这个 flag 直接 `+= 累计好感 × 2`
（走 `VNFlags.Add`，会触发属性 HUD 的飘字演出）。

用法：

```
if 闲聊_倾向_直球>=3 jump 太直接了
if 闲聊_好感变化>=4 jump 聊得非常好
if 闲聊_轮数<=3 jump 她好像没什么兴致
```

**结果名 vs flag 的分工**：结果名（好感提升/普通/冷场/失败）做**粗分支**，
flag 做**细判断**。别指望结果名能表达「她选了三次直球」这种信息。

---

## 八、option tone：三个选项的语气标签

每个候选回复都带一个隐藏标签，玩家看不见，但：

1. **约束 AI 生成**：强制三个选项走**实质不同**的方向，而不是同一句话的三种说法
2. **统计玩家倾向**：累计写进 flag，剧本可以据此分支
3. **塑造角色反应**：她对「直球」和「玩笑」的反应是不一样的

### 几条？3 或 5（最多 6）

`optionTones` 可以填 3~6 条，实际用几条由两个开关决定：

```
人格资产 useExtendedOptions  勾上 = 用满全部条数 / 关掉 = 只用前 3 条
剧本     options:N          临时覆盖（options:3 强制回到三选一）
```

所以**同一套人格可以两种玩法**：资产里填满 5 条，日常场合关掉开关走三选一（节奏快），
关键场合勾上或写 `options:5` 让玩家有更多选择。

> **前 3 条要放最核心的走向**——关掉扩展开关时只用前 3 条，
> 把「敷衍」这种边缘档放在第 1 位就浪费了。

选项面板会自动适配：**3 条及以下外观完全不变**；4 条起等比压缩条高、间距、字号，
总高压到 430px 以内（这是「不压到对话框」的几何上限，写死在 `VNChoicePanel.MaxTotalHeight`）。
实测 5 条时最下一条距对话框还有余量，立绘也基本没被挡。

**代价**：条数越多，AI 每轮要写的内容越多，也越容易让相邻两档写成近义句
（提示词里已经专门加了一句约束）。**但成本增加有限**——真正贵的是 `thinking`（见第十节）。

### 语气轴的设计

默认三档是 `温柔 / 玩笑 / 直球`，但这只是一种设计。换成别的会**整体改变玩法气质**：

**三档备选**

| 语气组 | 玩法变成 |
|---|---|
| `温柔 / 玩笑 / 直球` | 恋爱向，测「你想怎么接近她」（默认） |
| `赞同 / 追问 / 反驳` | 辩论向，测「你怎么看这件事」 |
| `安慰 / 转移话题 / 说实话` | 剧情向，适合她心情不好的场合 |
| `装傻 / 试探 / 摊牌` | 悬疑向，适合她在隐瞒什么的场合 |

**五档备选**（前 3 条即三档模式下用的那三条）

| 语气组 | 适合 |
|---|---|
| `温柔 / 玩笑 / 直球 / 反撩 / 敷衍` | **撩人型角色**（星野结衣现用）。「反撩」用她的方式回敬、「敷衍」不接招——两档都会让主动撩人的她失去主动权 |
| `温柔 / 玩笑 / 直球 / 认真 / 回避` | 通用恋爱向。「认真」推进关系、「回避」拖延，节奏可控 |
| `吐槽 / 起哄 / 附和 / 拆台 / 关心` | 损友向。日常插科打诨为主，「关心」是稀有的破防档 |
| `装傻 / 试探 / 追问 / 转移话题 / 摊牌` | 悬疑向。她在隐瞒什么的场合，五档正好是一条压迫感递增的轴 |
| `鼓励 / 建议 / 玩笑 / 拒绝 / 顺着她` | 养成向。适合她来找你商量事情的场合 |

设计要领：**每一档都要能触发她明显不同的反应**。如果两档得到的回应差不多，
那它们就该合并——五档写成五种同义说法，比三档还糟。

> 换语气组要改人格资产的 `optionTones`（必须正好 3 条），
> 顺带记得剧本里读的 `倾向_xxx` flag 名也会跟着变。

**一个设计上的甜点**：把某一档设成她**不擅长应对**的类型。
星野结衣是「明着撩人的辣妹」，所以「直球」那一档是她的克星——
她撩人时游刃有余，被认真回击就立刻乱套。这让三选一不再只是「说得更肉麻一点」，
而是真正的策略选择。

---

## 九、对话日志：怎么看，怎么用

每场聊完自动写两份到项目根的 `AiTalkLogs/`（已加进 `.gitignore`）：

```
2026-08-13_231458_星野结衣_日常.md     人读版，双击就能看
2026-08-13_231458_星野结衣_日常.json   机读版，给分析脚本
```

`.md` 的结构：

```
文件头   时间 / 人格 / 模型 / 剧本参数 / 轮数与历史配置（配错了这里会有 ⚠）
结果     结果名 / 累计好感 / 实际轮数 / 玩家倾向 / 写入的全部 flag
开销     总耗时 / 输入输出 token / 估算成本 / 平均每轮
对话     逐轮：她说了什么、表情漫符好感、三个选项、玩家选了哪个（→ 标记）
附录     首轮完整的 system prompt
```

### 调人格的正确姿势

1. 跑一场，留下日志 A
2. 改人格资产的某**一个**字段
3. 再跑一场，得到日志 B
4. 把两份日志的「附：首轮 system prompt」段落对比 → 确认改动真的进去了
5. 对比两份的对话正文 → 判断这个改动有没有起作用

**一次只改一个字段**，否则分不清是哪句话起了作用。

### JSON 能拿来做什么

每轮 17 个字段全是结构化的（`pickedIndex`、`tone`、`emotion`、`costUsd`…），
所以以后可以写脚本批量分析，比如：

- 哪种 `topic` 的平均好感增幅最高
- 玩家最常选哪种语气 → 反推该给谁加戏
- 哪几轮走了兜底（`degraded: true`）→ 网络稳不稳
- 一个月总共花了多少钱

### 不想写日志了

模块的 `logMode` 调成 `关闭`。默认是「仅编辑器」——发行版不会往玩家硬盘写东西。

---

## 十、成本与性能

> **怎么查账**：单场看日志的「开销」段；跨场看
> **Tools → VN Effects → AI → 花费报表 Cost Report**（按月/日/人格/模型/来源聚合，可导 CSV）。
>
> **单价改哪**：`VNAiPricingDef` 资产（Create → VN → AI Pricing）登记进
> `VNGameConfig.aiPricing`。不建资产就用内置默认表。
> **换了模型一定要确认表里有它**——查不到会按最贵档估算并在日志里标「单价存疑」。
>
> **一场 = 轮数 + 1 次请求**：收场总结（写记忆与日记）是一次独立请求，
> 约 $0.001，日志里单列一行。关掉人格的 `enableMemory` 就不会发。

以 `gemini-3.5-flash-lite`（$0.30 / $2.50 每百万 token）实测：

| 项 | 实测值 |
|---|---|
| 单轮延迟 | 1.3 ~ 2.0 秒 |
| 单轮 token | 约 1100（输入 ~950 + 输出 ~170） |
| 单轮成本 | 约 **$0.0007**（半分钱） |
| 8 轮一场 | 约 **$0.006**（四分钱） |
| 21 轮一场 | 约 **$0.015**（一毛钱） |

### ⚠ `thinking` 是最贵的一个开关

人格资产的 `thinking` 默认是 `Minimal`（不思考）。调成 `High` 之后实测：

| `thinking` | 延迟/轮 | 3 轮成本 | 思考 token/轮 |
|---|---|---|---|
| `Minimal`（默认） | ~1.5s | $0.002 | 0 |
| `High` | ~6s | **$0.0132** | 约 1470 |

**4 倍延迟、6 倍成本。** 思考 token 按输出价（$2.50/百万）计费，而且量很大——
它会盖过其他所有成本因素。

值不值得看场合：关键剧情、选项档位多、人设复杂时，`High` 的选项区分度确实更好；
日常闲聊用 `Minimal` 完全够。**它是本系统唯一一个能让成本翻数倍的开关，
调它之前先想清楚。**

> 顺带澄清一个容易搞错的归因：**候选回复从 3 条加到 5 条，成本几乎没变**。
> 看到成本暴涨先去查 `thinking`，不是查 `optionTones`。

### 省钱的杠杆（效果从大到小）

1. **`thinking` 调回 `Minimal`** —— 唯一能省掉数倍的开关
2. **`historyTurns` 调小** —— 输入 token 是大头，历史占了其中绝大部分
3. **`turns:` 调小** —— 直接少几轮
4. **`maxOutputTokens` 调小** —— 输出单价是输入的 8 倍，但量小，影响不如前面几个

**加速的杠杆**：目前是非流式（等完整 JSON 才开始打字机）。
配合「正在输入…」动画体感尚可。要更快得上流式（SSE），见 Ideas。

---

## 十一、Ideas：可以怎么玩

> 更完整的构想清单（A~J 十组、按当前前提排过优先级、含自由输入的完整设计）
> 见 **`AiTalkIdeas.md`**。这一节只留「不用改代码就能玩」的部分。

### 已经能做的（不用改代码）

- **一角色多人格**：`星野结衣_日常` / `星野结衣_约会` / `星野结衣_吵架后`
  共用同一套立绘，剧本用 `persona:` 切。三套人格的 `relationship` 写不同的距离感
- **好感门槛解锁**：`if 好感>=60` 才允许 `event aitalk persona:星野结衣_约会`
- **手写记忆**：`memory:她上次说过很讨厌打雷`，让她「记得」剧情里发生过的事
- **话题即玩法**：`topic:` 换成具体任务——`topic:说服她加入社团`、
  `topic:问出她周末的安排`，再用 `flag:` 判断有没有达成
- **换语气轴做不同场景**：吵架场合把 `optionTones` 换成 `道歉/辩解/沉默`

### 要写代码的（按性价比排序）

1. **跨场记忆** ⭐最值得做。聊完让 AI 生成一句摘要，写进独立 JSON
   （仿 `VNCgUnlocks` / `VNPhotoAlbum` 的全局存储），下次注入 `memory` 字段。
   「她记得你上次说喜欢猫」是这个系统最能打动人的一点。
   `VNAiContext.memory` 字段已经留好，只差存储层
2. **流式输出**：边收边打字机，体感快很多。要自定义 `DownloadHandlerScript` 解析 SSE
3. **道具联动**：把背包内容塞进 system prompt，送礼时她能有针对性反应
4. **AI 当裁判**：羽毛球 / 大头贴结束后，让她用角色口吻点评成绩（一次请求，复用现成 flag）
5. **AI 出题**：让 AI 生成题目喂给现成的 `VNQuizModule`，题库不再是死的
6. **中途存档**：需要改成非 event 的视图层（仿 `VNSnsView`）+ 走存档三处同步
7. **TTS 配音**：另一整套集成、另一笔钱、又一处延迟

---

## 十二、已知限制（设计如此，不是 bug）

| 限制 | 原因 | 缓解 |
|---|---|---|
| **不进翻译表** | AI 台词是实时生成的，进不了 FNV-1a 旁路表 | 按 `VNLocale.Language` 切提示词，让 AI 直接用目标语言写 |
| **没有配音** | 同上 | 只能上 TTS |
| **事件中不能存档** | 事件模块的通用约定 | 一场控制在合理轮数内；或将来改成视图层 |
| **每轮不进回想** | `EventCo` 只在结束后记一条 | 看对话日志（比回想还全） |
| **玩家可能断网** | 客观现实 | 兜底台词 + `* 失败` 分支。**这是主线不能依赖本模式的根本原因** |
| **Key 仅本地开发** | 打包进 Build 会泄漏 | 要发行必须改玩家自填或自建中转服务器 |

> **产品定位建议**：只用在番外 / 自由时间 / 好感达标后解锁的场合。
> 主线走手写剧本，AI 做增量——这样断网的玩家只是少一个模式，而不是卡在主线里。

---

## 十三、排错

| 症状 | 查这里 |
|---|---|
| 每轮都说兜底台词 | Tools → AI → Show Key Status 看 key；再跑 Test Gemini Connection 验网络 |
| 台词正常但立绘不动 | `event` 之前忘了 `show` 角色（Console 有告警） |
| 聊到后面答非所问 | `turns:` > `historyTurns`，看日志文件头的 ⚠ |
| 好感涨得离谱 | 调小 `affectionClamp`（代码已强制钳制并告警） |
| 选项点不动 | 模块自绘 UI 吃了射线——自绘的东西必须 `raycastTarget=false` |
| 她出戏 / 人设崩 | 规则写进 `boundaries`（位置最靠后，权重最高） |
| **她自己编出重大剧情**（怀孕/告白/转学…） | `boundaries` 里要列**明确清单**而不是抽象原则，再加一句「拿不准就换轻松话题」 |
| **成本突然涨了好几倍** | 先查 `thinking` 是不是被调成了 `High`（贵约 6 倍），不是查 `optionTones` |
| 选项条数不对 | 人格的 `useExtendedOptions` 与剧本 `options:N`；Console 有超范围告警 |
| 倾向 flag 互相覆盖 | `optionTones` 里有重名，Validate 会报 |
| 日志没生成 | `logMode` 是不是被改成「关闭」了 |

**三级诊断**（都不用进 Play Mode）：

| 菜单 | 验什么 |
|---|---|
| AI → Test Gemini Connection | key / 网络 / 模型名 |
| AI → Test Persona Talk (3 turns) | 提示词组装 / schema / 解析 / 多轮上下文 |
| AI → **AI Talk Studio** | 上面两条之外的一切——见第十六节 |

前者过、后者挂 = 问题在提示词或人格配置，不是网络。

---

## 十四、日记本（D 键）

每场聊完，AI 会用**主角的口吻**写一小段日记，攒进日记本。玩家按 **D** 翻看。

```
2026-08-14 02:12  ·  学校教室  ·  星野结衣

今天和结衣聊到了人生态度的话题，这丫头总是一副无忧无虑的样子。我顺势开了个
玩笑，让她以身相许来报答我。没想到她竟然害羞得语无伦次，慌乱的样子和平时大
大咧咧的她完全不同。看着她那种招架不住的可爱反应，我的心跳莫名漏跳了一拍。

# 人生态度  # 玩笑  # 以身相许    好感 +2
```

### 存储语义：和记忆**刻意相反**

| | 记忆 | 日记本 |
|---|---|---|
| 存哪 | `VNSaveData.aiMemories`（存档） | `persistentDataPath/vn_ai_diary.json`（全局） |
| 读旧档 | **跟着回退**——她不该记得未来 | **不回退**——玩家真经历过的不该消失 |
| 类比 | VNFlags | CG 画廊 / 大头贴相册 |

代价是可能出现「日记里有第 5 章的条目，但读回第 3 章的档时她不记得」。
这是刻意的：**日记是玩家的记录，记忆是角色的记忆，本来就不是一回事。**

### 为什么日记要 AI 写，而不是拼数据

拼出来的「你和星野结衣聊了 3 轮，好感 +2，话题：值日」是报表，不是日记。
让 AI 用主角口吻写一段，玩家读到的是**自己当时的心情**——这才是代入感的来源。
成本是零（和记忆摘要在同一次请求里出）。

### 一个踩过的坑：视角会跑偏

第一版写出来的日记是**她的口吻**（「哼，才不承认呢」），不是主角的。
在提示词里反复强调「用男方口吻」没有用。

**根因不在措辞**：总结请求把对话历史按 `role: user/model` 交替发过去，
她的台词标着 `model`——模型看到「自己说过这些话」，身份代入是结构性的。

**解法**：把对话**拍平成一段纯文本**（「星野结衣：…／我：…」）放进单条 user 消息。
模型于是只是在读一份别人的对话记录，身份代入自然消失，一次就对了。

> 这个坑值得记住：**当模型的身份认知不对时，先看消息的 role 结构，再改提示词措辞。**
> 结构性信号压得过任何措辞。

### 面板

- **D** 打开 / 关闭，**ESC** 也能关，点暗底同样关
- 最新的在最上；条目多于一个角色时顶部出现角色筛选标签
- 上限 200 条，超出丢最早的

### 相关字段

| 字段 | 作用 |
|---|---|
| 人格 `writeDiary` | 关掉 = 只攒记忆不写日记 |
| 人格 `enableMemory` | 关掉 = 记忆和日记都不做（总结请求也不会发） |

---

## 十五、繁体字兜底

提示词里写了「全部使用简体中文，禁止繁体字」，还把这条挪到了 system prompt
的**最后一行**（权重最高的位置）。但实测**仍然会漏**——三次抽查三次都混出了繁体
（「才、才沒有」「我还沒想好」「眞希望」）。模型对这种字级约束的遵守是概率性的，
提示词压不到 100%。

而这是玩家直接看得见的东西，所以加了一道**确定性的代码兜底**：
`VNAiTextNormalize.ToSimplified()`，覆盖中文 AI 输出里最常漏的那批高频字，
作用在台词、候选回复、日记、摘要、话题标签上。

**分工**：提示词负责让它「大部分时候写对」，兜底负责让它「永远不出错」。

漏了新字就往 `VNAiTextNormalize.Pairs` 里加一对——那个表是给兜底用的，
不追求学术完备（完整方案要 OpenCC 级词库）。命中时 Console 会告警，
方便你知道该不该加强提示词。

> 繁→简方向是安全的：多个繁体字映射到同一个简体字不会产生歧义。
> 危险的是反方向（简→繁的一对多），本项目不做那个方向。

---

## 十六、AI 试聊台（调人格与提示词的主力工具）

**Tools → VN Effects → AI → AI 试聊台 AI Talk Studio**（人格资产上右键 →「在 AI 试聊台里打开」也行）

不进 Play Mode、不跑剧本，直接改人格、试聊、看提示词、调记忆。
第九节那套「跑一场留日志 → 改一个字段 → 再跑一场 → 肉眼 diff」的流程，
大部分可以被它取代。

### 三栏分别干什么

```
┌ 顶栏  人格 · 草稿已改 N 项 [写回资产][还原] · 轮数/耗时/token/成本 ───┐
├ 左：改参数 ──────┬ 中：聊天流 ────────┬ 右：prompt / JSON / 诊断 ┤
│ 情境（=剧本参数）│ 她说的话 + 元信息   │ system prompt 实时预览   │
│ 人格草稿（全字段）│ 候选回复（可点）    │ 本轮原始 JSON            │
│ 记忆池（可增删改）│ 自由输入框          │ responseSchema           │
└──────────────────┴────────────────────┴──────────────────────────┘
```

### 四个最省时间的用法

| 想干嘛 | 怎么做 |
|---|---|
| **改提示词看拼装结果** | 左栏改 `boundaries` / `speechStyle` → **右栏立刻重拼**。不发请求、不花钱、不等 1.5 秒。第九节第 4 步（对比两份日志确认改动进去了）可以直接省掉 |
| **验证某种回复会怎样** | 底部输入框直接打字（Ctrl+Enter 发送），绕开三选一。想试「直球会不会让她乱套」不用等 AI 刚好生成那种选项 |
| **判断改动有没有效果** | 「↻ 重跑本轮」同样输入连发几次。单看一次结果分不清是参数起作用还是模型本来就会飘 |
| **看记忆到底有没有用** | 左栏记忆区的「把记忆注入 prompt」勾掉再跑一遍，直接对比 |

另外每一轮右上角有「↺ 从这重来」——丢掉这一轮和后面的重新分岔，
前面聊过的不用重来。点**非最后一轮**的候选回复也是同样效果。

### 草稿层：改参数不动资产

左栏改的是一份**内存副本**，磁盘上的 `.asset` 一动不动。改过的字段名前面标 ●，
顶栏显示「草稿已改 N 项」并列出 `旧值 → 新值`。满意了点「写回资产」（带 Undo，
Ctrl+Z 能撤销），不满意点「还原」。

好处是调参过程不再污染 Git，也不会出现「调完忘了改回来，第二天纳闷她怎么变了」。

### 记忆预设

存在 `<项目根>/AiTalkStudio/Memories/*.json`（不进 git），可以存多套并随时切换：

```
初次见面（空）      ← 验证她第一次见面的表现
聊过 3 次           ← 验证记忆注入后的连续性
好感 80 的老朋友     ← 配合左栏好感滑杆一起用
```

**和游戏里的记忆是两回事**：运行时那份跟着存档走（读旧档她就不记得未来），
试聊台这份是独立的调试场景，绝不会写进存档。

条目来源有四个：手写、试聊完点「结束并总结」（结果先给你看，**收下**才进池子）、
从 `AiTalkLogs` 日志导入、从游戏存档槽导入。

> 从日志导入会**发一次总结请求**（约 $0.001）。因为日志里没有 summary/topics/facts
> （总结不写进日志），不重新总结的话导进来是一条空壳，注入了等于没注入。

### 试聊日志

结束时写到 `AiTalkLogs/Editor/`，**格式与游戏内日志完全一致**（md + json），
所以试聊产生的会话可以和真实游玩记录互相对比。不想写就在左栏关掉。

### 成本

顶栏常驻累计 `$`，超过 $0.05 变红。草稿把 `thinking` 调离 `Minimal` 时顶栏会直接
标红警告——试聊台鼓励反复重跑，配上 `High` 很容易一下午烧掉几块钱（见第十节）。

### 已知限制

- **没有 A/B 并排对比**（要对比只能先后跑两次，或翻 `AiTalkLogs/Editor/` 的两份日志）
- **没有演出预览**：立绘表情、漫符、打字机都看不到，纯文本。表情穿帮要实际跑剧本才发现
- **域重载会打断在飞的请求**（改代码 / 进 Play Mode）。对话历史会自动重建，
  提示「已中断」，重跑一轮即可

---

## 十七、两家供应商：Gemini ⇄ DeepSeek

（2026-08-17 加。**默认已换成 DeepSeek `deepseek-v4-flash`**。）

### 怎么切

三层，越下面越优先：

| 层 | 在哪 | 用途 |
|---|---|---|
| **全局默认** | `VNGameConfig` →「AI 默认供应商 / 默认模型」 | **一处改，全部人格跟着换**。日常换家改这里 |
| **人格资产** | `VNAiPersonaDef` →「供应商 / 模型名」 | 默认是「跟随全局」。某个角色要单独用贵模型时才填 |
| **试聊台** | 顶栏的供应商 + 模型两个格子 | 改的是**草稿**，同一句话用两家各跑一遍对比效果和成本，不写回资产就不影响游戏 |

模型名留空 = 那家的默认模型（Gemini `gemini-3.5-flash-lite` / DeepSeek `deepseek-v4-flash`）。
**填错家的模型名会 400**，人格资产的自检（Validate）会先拦下来。
试聊台切供应商时会自动清掉不属于新那家的模型名。

自检：**Tools → VN Effects → AI → 测试连接·当前默认供应商 Test Connection · Gemini / · DeepSeek**
分别验两家的 key 与网络；`Show Key Status` 一次列出两家 key 找没找到。

### 两家的差异（会影响你怎么调参）

| | Gemini | DeepSeek |
|---|---|---|
| 端点 / 鉴权 | `generativelanguage…:generateContent`，`x-goog-api-key` 头 | `api.deepseek.com/chat/completions`，`Authorization: Bearer` |
| 角色名 | `user` / `model` + 独立 `systemInstruction` | `system` / `user` / `assistant`（system 是 messages 第一条） |
| **结构化输出** | `responseSchema`：enum、条数都是**服务端硬约束** | 只有 `json_object` 模式，**没有硬约束** |
| 内容安全 | `safetySettings` 可放宽到 BLOCK_ONLY_HIGH | **没有这个参数**，人格资产的 `safety` 对它无效 |
| 思考 | `thinkingLevel` 四档 | 开关 + `reasoning_effort` 三档（映射：Minimal→关，Low→low，Medium→high，High→max） |
| 计费 | 全天同价 | 分**缓存命中/未命中**、分**高峰/非高峰**（高峰翻倍） |

### ⚠ 最重要的一条：DeepSeek 的格式约束是「软」的

Gemini 那边 `emotion` 是 enum，模型**物理上编不出**不存在的表情名，
`options` 也必然正好 N 条。DeepSeek 只能靠提示词说，所以：

- system prompt 末尾会**多出一段「输出格式」**（键名、类型、带 tone 的示例 JSON）。
  在试聊台右栏看得到——切到 Gemini 它就消失了，那是正常的
- 兜底靠 `VNAiConversation.TryParseTurn`：表情越界降级成第一个合法表情、
  选项不足自动补齐、好感强制 Clamp。**演出会打折，但不会崩**
- 排错顺序：右栏「原始 JSON」看模型实际返回了什么 → 是漏字段还是编了个新表情

#### 已修复的一个必现坑：第 2 轮起「回复正文为空」

`json_object` 模式下，如果历史里 **assistant 的消息是纯文本**，模型会照着
「我上一条说的是纯文本」继续，可 JSON 模式又只准它出 JSON —— 于是退化成
**吐一串空白字符**（`finish_reason=stop`，content 是 20 个空格）。
第 1 轮没有 assistant 历史所以正常，**第 2 轮起必挂**，
表现出来却像是「时好时坏的失败」。

对照实验（deepseek-v4-flash，2026-08-17）：

| 历史里的助手消息 | 思考 | 结果 |
|---|---|---|
| 纯文本 | 开 / 关 | ❌ 两次都返回 20 个空格 |
| 完整 JSON | 开 / 关 | ✅ 两次都正常 |
| 只包 `{"reply":"…"}` | 开 / 关 | ✅ 三次都正常 |

**和 `thinking` 无关**。修法取最省 token 的那个：
`VNAiConversation.AppendHistory()` 在没有硬 schema 的家上，把历史里她说过的话
包成 `{"reply":"…"}` 再发（每条多约 12 token）。`_history` 本身仍只存纯台词——
收场总结要把对话拍平成记录、试聊台域重载要靠 reply 重建历史，共用同一份数据。

### 价格对比（每百万 token，2026-08）

| 模型 | 输入 | 输出 | 缓存命中输入 |
|---|---|---|---|
| Gemini 3.5 Flash Lite | $0.30 | $2.50 | —— |
| **DeepSeek V4 Flash（默认）** | **$0.22** | **$0.66** | **$0.007** |
| DeepSeek V4 Pro | $0.66 | $1.98 | $0.022 |

DeepSeek 标的是**非高峰价**，高峰时段（UTC 01–04、06–10）翻倍——
`VNAiPricingDef` 的 `peakMultiplier` 与「高峰时段」列表就是干这个的，
官方改时段时改资产即可，不用动代码。

**缓存命中价便宜约 30 倍**，而我们每轮都重发整段 system prompt + 历史，
正好是命中率最高的场景。日志的「开销」表里会单列一行命中率：
命中率低说明 system prompt 或历史前缀一直在变（比如每轮都改情境参数）。

> 换家之后旧日志照样能看：日志里存了模型名与供应商，
> Cost Report 的「按当前单价重算」会用对应那家的单价，
> 高峰倍率按**那场对话当时**的时间判，同一份日志今天看和明天看是同一个数字。
