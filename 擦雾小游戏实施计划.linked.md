# 擦雾小游戏实施计划

> **状态**：方案已定案（三轮问答共 11 项决策全部拍板），**尚未开工**
> **撰写日期**：2026-09-01
> **目标**：VN 剧本流程中可以 `event wipefog` 拉起一段「擦拭起雾玻璃 → 看清 CG」的小游戏，
> 清晰度达标情况回流剧情分支
> **建议分支**：`feature/fog-wipe-minigame`（用户发话后再切）

---

## 目录

- [一、玩法定性与乐趣内核](#一玩法定性与乐趣内核)
- [二、已定案的十一项决策](#二已定案的十一项决策)
- [三、参数数学（难度是可以算出来的）](#三参数数学难度是可以算出来的)
- [四、技术方案](#四技术方案)
- [五、文件清单](#五文件清单)
- [六、资产字段设计（VNFogWipeDef）](#六资产字段设计vnfogwipedef)
- [七、剧本语法设计](#七剧本语法设计)
- [八、分阶段实施计划](#八分阶段实施计划)
- [九、风险清单与保险丝](#九风险清单与保险丝)
- [十、被否决的方案与理由](#十被否决的方案与理由)
- [十一、工时估算](#十一工时估算)

---

## 一、玩法定性与乐趣内核

### 1.1 擦雾本身不是乐趣，是劳动

所有擦除类玩法的乐趣只可能来自三处：

| 乐趣来源 | 机制本质 |
|---|---|
| **揭示 Reveal** | 信息被逐步释放，释放速度由玩家掌握 |
| **对抗 Resistance** | 你擦，它回来；跟系统赛跑 |
| **表达 Expression** | 擦出的痕迹是玩家的作品，被系统读取并回应 |

**只有揭示的话，玩法寿命约 20 秒**——玩家擦两下发现「哦只要擦就行」，剩下全是体力活。
这是这类玩法翻车最常见的原因，本方案的每一条设计都在对抗它。

### 1.2 本作采用：揭示 + 轻度对抗

- **揭示**：雾 = CG 的模糊提亮版，剪影透出 → 玩家看得见「那边有个人影」，自然知道往哪擦
- **对抗**：雾会重新凝结（边缘侵蚀 + 随机雾团），玩家要在硬时限内把清晰度推过阈值
- **不采用表达**：本版不做「在雾上写字给她看」，留作后续扩展（见第十节）

### 1.3 ★ 一个必须写在最前面的设计风险

已定案的组合是「**回雾对抗**」+「**擦到就掉、不设速度惩罚**」+「**面积就是挑战**」。

这意味着难度的**唯一**来源是「鼠标划过的面积 vs 回雾速度」，也就是**纯手速体力活**。
后果只有两种，取决于参数：

- 回雾调慢 → 玩家疯狂甩鼠标几秒通关，对抗形同虚设
- 回雾调快 → 变成必须一直高速甩鼠标的累人活，且依然没有技巧、只有体力

**这不是设计错误，是取向选择**（用户要的就是「操作上轻松」）。
但它把全部成败压到了参数上——所以第三节的数学**必须算准**，
并且第八节 P4 有一个**编辑器实时调参预览**是硬要求，不是加分项。

---

## 二、已定案的十一项决策

| # | 决策项 | 结论 | 影响 |
|---|---|---|---|
| 1 | 玩法形态 | **清晰度达标 + 回雾对抗** | 结束条件 = 时限到 or 达标；HUD = 清晰度条 + 倒计时 |
| 2 | 角色是否实时反应 | **在场，但只是被动 CG** | **不接部位框/表情/立绘**，模块完全不碰舞台 |
| 3 | 投入规模 | **正式可复用模块** | 做资产 + DSL + 装机器 + Lint，不做常驻演出组件 |
| 4 | 手感取向 | **轻松，擦到就掉，无速度惩罚** | 不实现速度反比曲线；风险见 1.3 |
| 5 | 回雾形态 | **边缘侵蚀 + 随机雾团** | 掩码每帧衰减用预算权重图；定时盖高斯团 |
| 6 | 张力来源 | **面积就是挑战**（仅此一项） | 不做目标区域隐藏、不做道具取舍 |
| 7 | 时限与结果 | **硬时限 + 三档结果** | 完美 / 普通 / 失败；失败是剧情分支不是 Game Over |
| 8 | 雾的外观 | **底图模糊提亮，剪影透出** | shader 内做 9-tap 模糊，不用预烘焙贴图 |
| 9 | 光标素材 | **程序化生成** | [`VNProceduralTextures`](Assets/Project/Scripts/VNEffects/VNProceduralTextures.cs) 新增雨刷/抹布形状；资产留自定义 Sprite 槽 |
| 10 | 音效 | **代码合成** | 参照 [`VNBadmintonSfx`](Assets/Project/Scripts/VNEffects/Script/VNBadmintonSfx.cs) / [`VNPhotoSfx`](Assets/Project/Scripts/VNEffects/Script/VNPhotoSfx.cs)；音量音调跟鼠标速度联动 |
| 11 | 分阶段台词 | **写在资产里** | [`VNFogWipeDef`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeDef.cs) 配阈值 + 台词池 + 语音 id，照抄 [`VNInteractionDef`](Assets/Project/Scripts/VNEffects/Script/VNInteractionDef.cs) 结构 |

### 决策 2 与 3 带来的两个架构简化（重要）

**因为角色是被动 CG**：

1. **模块完全不碰舞台**，不需要像 [`VNInteractionModule`](Assets/Project/Scripts/VNEffects/Script/VNInteractionModule.cs) / [`VNAiTalkModule`](Assets/Project/Scripts/VNEffects/Script/VNAiTalkModule.cs) 那样破「模块三铁律」。
   不接立绘、不换表情、不管眨眼口型、无需还原原表情、无需 `ClearImprints()`。
2. **雾层可以整个建在事件层 60 内部**（雾在最底、HUD 与台词条在其上，靠子物体顺序分层），
   **不需要**拆出常驻的 `VNFogWindow` 组件，因此**不用走 vn-save-compat 三处同步**。

代价与副作用：

- 没有 `fx fog on/off` 纯演出命令（将来真需要再补，见第十节）
- 过程中的台词**由模块自绘一条轻量台词条**——对话框在排序 40，会被 60 层的雾盖住。
  自绘反而更好：台词条能贴着 CG 排版，视觉上像「镜面上的旁白」。

---

## 三、参数数学（难度是可以算出来的）

既然难度唯一来自「屏幕大、笔刷小、雾会回来」，它就是一道能算的题。

### 3.1 擦除速率

```
每秒擦除面积 ≈ 笔刷直径 D × 鼠标移动速度 v
```

参考分辨率 1920×1080 = **2,073,600 px²**：

| 笔刷直径 D | 拖动速度 v | 每秒覆盖 | 占全屏 | 理论擦满 | 含重叠浪费实际 |
|---|---|---|---|---|---|
| 120 px | 800 px/s | 96,000 | 4.6 % | 22 s | **32~38 s** |
| **180 px** | **800 px/s** | **144,000** | **6.9 %** | **14 s** | **20~25 s** |
| 240 px | 1200 px/s | 288,000 | 13.9 % | 7 s | **10~13 s** |

> 「含重叠浪费」按 1.5 倍估算：玩家不可能画出完美不重叠的覆盖路径。

### 3.2 叠加回雾后的净推进

设回雾速率 R（%/秒），净推进 ≈ 覆盖率 − R：

| R | 净推进 | 擦到 80% 需时 | 60 秒时限下的体感 |
|---|---|---|---|
| 2.0 %/s | +2.6 %/s | 31 s | 很宽松，几乎必过，对抗形同虚设 |
| **3.0 %/s** | **+1.6 %/s** | **50 s** | **卡在时限内，紧张但能过** ← 推荐 |
| 3.5 %/s | +1.1 %/s | 73 s | 过不了，纯挫败 |

### 3.3 ★ 资产默认值（写进 VNFogWipeDef 的出厂设定）

```
笔刷直径        170 px      （相对 1920 宽 ≈ 8.9%）
笔刷柔边        35 %        （半径的 35% 作为羽化带）
单次擦除强度    0.85        （一次划过不到全清，需要复擦 → 保留「越擦越亮」的曲线）
回雾总速率      3.0 %/s
  ├ 边缘侵蚀    2.0 %/s     （从四周往中间吞）
  └ 随机雾团    1.0 %/s     （每 0.8~1.5 s 在中心区盖一个高斯团）
时限            60 s
三档阈值        完美 ≥ 90% / 普通 ≥ 65% / 失败 < 65%
掩码分辨率      256 × 144
```

**这三个数（笔刷直径、回雾速率、达标阈值）必然要反复试玩微调，一次调对的概率为零。**
所以 P4 的编辑器调参预览是刚性需求。

---

## 四、技术方案

### 4.1 核心：低分辨率掩码贴图

```
掩码 = Texture2D(256, 144, TextureFormat.R8, mipChain: false)
       filterMode = Bilinear
```

**关键洞察：雾本身就是模糊的，掩码不需要高分辨率。** 双线性插值放大 7.5 倍后完全看不出来。

256×144 = **36,864 字节**（R8 每像素 1 字节）：

- 用 `SetPixelData<byte>()` 而非 `SetPixels32()`——数据量只有 1/4，且免去 Color32 装箱
- 每帧全掩码遍历（回雾）= 3.7 万次 byte 运算，**开销可忽略**
- 清晰度统计 = 同一次遍历里顺手求和，零额外成本
- 对照：[`VNPhotoDoodle`](Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs) 用 768×576 的 Color32 画布都能每帧 Apply 且不掉帧

### 4.2 擦除笔刷 —— 直接搬 VNPhotoDoodle

`Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs` 里这三段可以整段复用：

| 现有方法 | 复用方式 |
|---|---|
| `StrokeTo(uv)` | **原样搬**——沿线段按 `penSize*0.3` 步长补点，防止快速拖动画出一串断圆 |
| `Stamp(center)` | 改：把 [`Paint()`](Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs#L283)（朝笔色 lerp）换成「掩码累加 `mask = max(mask, strength)`」 |
| `Flush()` | 原样搬——脏标记 + `Apply(false)`，不脏不传 |
| `VNPhotoDoodleInput.ToUv()` | **原样搬**——`ScreenPointToLocalPointInRectangle` 换 uv |

**这部分等于已经写好了，是本项目最大的一块现成收益。**

### 4.3 回雾算法

#### 边缘侵蚀（2.0 %/s）

预先算一张 **`_edgeWeight[]`（256×144 的 float 或 byte）**：
每个像素到最近边缘的归一化距离 `d`，权重 `w = 1 - smoothstep(0, 0.35, d)`。
即越靠边权重越接近 1、中心接近 0。**只算一次，之后每帧零成本**。

每帧：`mask[i] -= edgeRate * _edgeWeight[i] * dt`

#### 随机雾团（1.0 %/s）

每隔 `Random.Range(0.8f, 1.5f)` 秒，在中心区（避开最外圈 15%）随机取一点，
盖一个半径 60~110 px 的高斯团 —— **就是反向的 `Stamp()`，代码同一份，符号取反**。

雾团出现时给一个轻微的白色脉冲提示（不然玩家不知道那边又糊了）。

#### 数值稳定

`mask` 是 byte，累减容易被整数截断吃掉小量。做法：内部维护 `float[] _maskF`，
每帧写回 byte 数组再上传。多一份 147KB 内存，换来数值正确，值得。

### 4.4 雾层渲染 —— 新 shader `VN/FogWipe`

```hlsl
_MainTex   当前 CG（模块从 VNStage 拿；或剧本 cg: 覆盖）
_MaskTex   擦除掩码（R8）
_NoiseTex  程序化雾噪声（边缘破碎 + 水汽颗粒）
_FogColor  雾色（默认接近白，带一点冷调）
_FogDensity / _BlurRadius / _Brightness / _EdgeNoise
```

片元逻辑：

```
blurred = Blur9Tap(_MainTex, uv, _BlurRadius);        // 参考 VNImageEffect.shader 现成实现
fogCol  = lerp(blurred, _FogColor, 0.55) * _Brightness;

m = tex2D(_MaskTex, uv).r;
n = tex2D(_NoiseTex, uv * 3.0).r;
a = smoothstep(m - _EdgeNoise * n, m + 0.08, 0.5);    // 噪声扰动边界 → 破碎成水汽形状

return lerp(clearCol, fogCol, a * _FogDensity);
```

**两个必须做对的点**：

1. **边界一定要用噪声扰动**。掩码直接当 alpha 用会得到光滑的圆形边界，非常假。
   这一条对真实感的贡献超过任何其他单项。
2. **雾色取底图的模糊提亮版，而不是纯白**。这既让剪影透出（视觉引导，解决「不知道擦哪」），
   也让画面好看一个档次。纯白半透明 Image 会显得很廉价。

> 注意 `_UVRect` 坑：底图若是图集 Sprite，texcoord 不是 0~1（[`VNScreenTransition`](Assets/Project/Scripts/VNEffects/VNScreenTransition.cs) 踩过）。
> 本模块用 `RawImage` + `Texture` 而非 `Image` + `Sprite`，从源头绕开。

### 4.5 层级与射线

```
EventLayer (sortingOrder 60)
└─ VNFogWipeModule (RectTransform 铺满)
   ├─ [0] FogLayer   RawImage  raycastTarget = false
   ├─ [1] InputPad   Image     raycastTarget = true   ← 唯一吃射线的
   ├─ [2] LineBar    自绘台词条 raycastTarget = false
   ├─ [3] Hud        清晰度条 + 倒计时  raycastTarget = false
   └─ [4] Cursor     VNTouchCursor       raycastTarget = false
```

- 事件层 60 在对话框 40 之上 → **雾会盖住对话框**，所以台词条必须自绘（见决策 2 的副作用）
- 除 InputPad 与 ESC 确认框外，一切 `raycastTarget = false`（项目铁律）

### 4.6 光标

复用 [`VNTouchCursor`](Assets/Project/Scripts/VNEffects/Script/VNTouchCursor.cs)（已有跟随 / 摆动 / 按住震动 / 速度倾斜 / 悬停 HDR 发光）。

> **★ 血的教训（[`VNInteractionModule`](Assets/Project/Scripts/VNEffects/Script/VNInteractionModule.cs) 已踩过）**：`Dispose()` 必须在
> **Finish / CancelForDebug / OnDestroy / OnDisable 四处都调**，
> 漏掉任何一条，玩家的系统鼠标指针会**永久消失**。

雨刷/抹布图形由 [`VNProceduralTextures`](Assets/Project/Scripts/VNEffects/VNProceduralTextures.cs) 新增方法程序化生成，
资产里留 `customCursor` Sprite 槽，以后有美术图直接换上。

### 4.7 音效（代码合成）

新建 [`VNFogSfx`](Assets/Project/Scripts/VNEffects/Script/VNFogSfx.cs)，参照 [`VNBadmintonSfx`](Assets/Project/Scripts/VNEffects/Script/VNBadmintonSfx.cs) / [`VNPhotoSfx`](Assets/Project/Scripts/VNEffects/Script/VNPhotoSfx.cs)：

- **擦拭循环音**：白噪声 + 带通滤波，`AudioSource.pitch` 与 `volume` 跟鼠标速度联动
  （速度 0 → 静音；速度快 → 音调高、音量大）。这是「无速度惩罚」下唯一的速度反馈，很重要
- **雾团生成**：一声闷闷的低频「噗」
- **达标瞬间**：上扬的清脆音 + 亮度脉冲
- **倒计时最后 5 秒**：每秒一响（[`VNQuizModule`](Assets/Project/Scripts/VNEffects/Script/VNQuizModule.cs) 有先例）

### 4.8 与现有系统的联动

| 需求 | 走现成的 |
|---|---|
| CG 来源 | `VNStage.CurrentCgId`（[`Assets/Project/Scripts/VNEffects/Script/VNStage.cs:1213`](Assets/Project/Scripts/VNEffects/Script/VNStage.cs#L1213)），剧本 `cg:` 参数可覆盖 |
| CG 解锁 | [`VNCgUnlocks`](Assets/Project/Scripts/VNEffects/Script/VNCgUnlocks.cs)（擦到「完美」才解锁差分版本，可选） |
| 属性加成 | `stat:` / `rate:` 参数，接口与 `event photo` / `event aitalk` 一致 |
| 结果弹窗 | 可选接 `event result`，或模块内置简化结算 |
| 教程 | `VNEventModule.tutorialId`（[`Assets/Project/Scripts/VNEffects/Script/VNEventModule.cs:56`](Assets/Project/Scripts/VNEffects/Script/VNEventModule.cs#L56)） + [`VNTutorialPlayer`](Assets/Project/Scripts/VNEffects/Script/VNTutorialPlayer.cs) 已在基类（**正好补上本方案缺的引导**） |
| 回想 | `RecordInBacklog => true`（默认即可） |

---

## 五、文件清单

### 5.1 新增 —— 运行时（`Assets/Project/Scripts/VNEffects/Script/`）

| 文件 | 预估行数 | 职责 |
|---|---|---|
| [`VNFogWipeDef.cs`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeDef.cs) | ~200 | 资产定义：雾外观 / 笔刷 / 回雾三参数 / 阶段台词 / 三档阈值 |
| [`VNFogMask.cs`](Assets/Project/Scripts/VNEffects/Script/VNFogMask.cs) | ~220 | **纯逻辑，无 MonoBehaviour**：掩码缓冲、擦除 Stamp、边缘权重图、回雾衰减、清晰度求和 |
| [`VNFogScore.cs`](Assets/Project/Scripts/VNEffects/Script/VNFogScore.cs) | ~120 | **纯静态判定数学，可单测**：阶段推进、三档结果判定（结构照抄 [`VNTouchScore`](Assets/Project/Scripts/VNEffects/Script/VNTouchScore.cs)） |
| [`VNFogWipeModule.cs`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeModule.cs) | ~600 | 事件模块：UI 搭建、输入、HUD、台词条、倒计时、ESC、结算、flag 回写 |
| [`VNFogSfx.cs`](Assets/Project/Scripts/VNEffects/Script/VNFogSfx.cs) | ~150 | 五个代码合成音效 |

> **分层理由**：`VNFogMask` 与 `VNFogScore` 不继承 MonoBehaviour，
> 是因为手感全靠调这里的数，而调数需要能脱离场景反复跑。
> 这是项目里 [`VNPhotoScore`](Assets/Project/Scripts/VNEffects/Script/VNPhotoScore.cs) / [`VNBadmintonBallistics`](Assets/Project/Scripts/VNEffects/Script/VNBadmintonBallistics.cs) / `VNTouchScore` 的一贯习惯。

### 5.2 新增 —— 编辑器（`Assets/Project/Scripts/VNEffects/Editor/`）

| 文件 | 预估行数 | 职责 |
|---|---|---|
| [`VNFogWipeInstaller.cs`](Assets/Project/Scripts/VNEffects/Editor/VNFogWipeInstaller.cs) | ~120 | 场景装机：Tools → VN Effects → 场景装机 → **擦雾 Fog Wipe**（照抄 [`VNQuizInstaller`](Assets/Project/Scripts/VNEffects/Editor/VNQuizInstaller.cs)） |
| [`VNFogTuneWindow.cs`](Assets/Project/Scripts/VNEffects/Editor/VNFogTuneWindow.cs) | ~250 | **调参预览窗口**：不进 Play Mode 拖笔刷试擦、实时看回雾、算出「预计通关秒数」 |

### 5.3 新增 —— Shader 与资产

| 路径 | 内容 |
|---|---|
| `Assets/Art/Shaders/VNFogWipe.shader` | 雾层 shader（9-tap 模糊 + 掩码 + 噪声边界） |
| `Assets/Art/VNEffects/FogWipe/*.asset` | 至少一个示例 `VNFogWipeDef`（浴室镜面） |

### 5.4 改动清单

| 文件 | 改动 |
|---|---|
| [`VNProceduralTextures.cs`](Assets/Project/Scripts/VNEffects/VNProceduralTextures.cs) | 新增 `WiperSprite` / `ClothSprite` / `FogNoise` 三个程序化贴图 |
| [`VNGameConfig.cs`](Assets/Project/Scripts/VNEffects/Script/VNGameConfig.cs) | 新增 `public List<VNFogWipeDef> fogWipes`（玩法页） |
| [`VNGameConfigEditor.cs`](Assets/Project/Scripts/VNEffects/Editor/VNGameConfigEditor.cs) | 把 `fogWipes` 登记进「玩法」页签 |
| [`VNStage.cs`](Assets/Project/Scripts/VNEffects/Script/VNStage.cs) | 只读暴露当前 CG 的 `Texture`（若尚无此入口） |
| [`VNScenarioSchema.cs`](Assets/Project/Scripts/VNEffects/Editor/VNScenarioSchema.cs) | 登记 `event wipefog` 的参数与下拉候选 |
| [`VNScenarioLinter.cs`](Assets/Project/Scripts/VNEffects/Editor/VNScenarioLinter.cs) | 检查：`id:` 是否已登记、`* 失败` 是否被接住 |
| [`VNEventRegistry`](Assets/Project/Scripts/VNEffects/Script/VNEventRegistry.cs)（场景） | 装机器自动登记模板，无需手改代码 |

### 5.5 明确**不**改动

- [`VNSaveData`](Assets/Project/Scripts/VNEffects/Script/VNSaveSystem.cs) / `RestoreSnapshot` / `RebuildStateBefore` —— **模块状态不进存档**
  （event 模块中途本就不能存档；雾层随模块销毁，没有「读档后该不该还在」的问题）
- 舞台演出相关的一切（[`VNCharacterOverlay`](Assets/Project/Scripts/VNEffects/Script/VNCharacterOverlay.cs) / [`VNSpeakerHighlight`](Assets/Project/Scripts/VNEffects/VNSpeakerHighlight.cs) / 表情系统）
  —— 决策 2 定了角色是被动 CG，模块不碰舞台
- 对话框 [`VNDialogueBox`](Assets/Project/Scripts/VNEffects/VNDialogueBox.cs) —— 台词条自绘，不动它

---

## 六、资产字段设计（VNFogWipeDef）

```
[雾的外观]
  fogColor            Color     雾色（默认 (0.93, 0.95, 0.98)）
  fogDensity          0~1       雾的不透明度上限（默认 0.92）
  blurRadius          px        底图模糊半径（默认 6）
  brightness          倍率      模糊后提亮（默认 1.25）
  edgeNoise           0~1       边界破碎程度（默认 0.35）★ 调小会变成假的圆形边

[笔刷]
  brushDiameter       px        默认 170
  brushFeather        0~1       羽化带占半径比例，默认 0.35
  wipeStrength        0~1       单次划过的擦除量，默认 0.85
  cursorKind          枚举      雨刷 / 手掌 / 抹布 / 手指
  customCursor        Sprite    留空则用程序化图

[回雾]
  edgeRate            %/s       边缘侵蚀，默认 2.0
  blobRate            %/s       随机雾团，默认 1.0
  blobIntervalMin/Max s         默认 0.8 / 1.5
  blobRadiusMin/Max   px        默认 60 / 110

[规则]
  timeLimit           s         默认 60（剧本 time: 覆盖）
  targetPerfect       %         默认 90（剧本 target: 覆盖普通档）
  targetNormal        %         默认 65
  outcomePerfect/Normal/Fail  string  默认「完美」「普通」「失败」

[分阶段台词]  ← 结构照抄 VNInteractionFeedback
  stages[] {
      note            备注（长列表里认出这条）
      threshold       %      触发清晰度（如 30 / 60 / 90）
      lines           台词池（随机抽一条；三语走 VNScriptLocale）
      voicePool       语音 id 池
      blocking        bool   是否暂停擦拭等玩家推进（默认 false，不打断节奏）
      scriptLines     可写 fx / camseq 等演出命令（控制流命令白名单挡掉）
  }
```

---

## 七、剧本语法设计

### 7.1 典型用法

```
# 浴室镜面：她在里面，你在外面擦镜子
cg 浴室镜面
event wipefog id:浴室镜面 time:60 target:70 stat:好感 rate:0.08 flag:擦雾1
* 完美
    结衣　「…看清楚了？笨蛋。」
    imprint 星野结衣 掌印 pos:0.5,0.6
* 普通
    结衣　「差不多就得了啦。」
* 失败
    旁白　雾又聚拢起来，什么都看不清了。
```

```
# 车窗：不给失败分支（Lint 会提醒，但两档也合法）
cg 雪夜车窗
event wipefog id:雪夜车窗 time:45 target:60
* 完美
* 普通
```

### 7.2 参数表

| 参数 | 必填 | 说明 |
|---|---|---|
| `id:` | ✅ | [`VNFogWipeDef`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeDef.cs) 资产 id（在 `VNGameConfig.fogWipes`（[`Assets/Project/Scripts/VNEffects/Script/VNGameConfig.cs:214`](Assets/Project/Scripts/VNEffects/Script/VNGameConfig.cs#L214)） 登记） |
| `cg:` | | 指定要擦的 CG；留空 = 用舞台当前显示的 CG |
| `time:` | | 时限秒数，覆盖资产默认 |
| `target:` | | 「普通」档阈值（%），覆盖资产默认 |
| `perfect:` | | 「完美」档阈值（%） |
| `stat:` / `rate:` | | 清晰度换算成属性加成，接口同 `event photo` / `event aitalk` |
| `flag:` | | 成绩 flag 前缀 |
| `tutorial:` | | 覆盖模块默认教程 id（基类已支持） |

### 7.3 结果名（精确字符串匹配，永不翻译）

```
完美 / 普通 / 失败
```

> **`* 失败` 建议接住**。虽然不像 `aitalk` 的断网那样是硬性问题，
> 但玩家手残时会静默跳过整段剧情。Lint 给**警告**而非错误。

### 7.4 回写的 flag

| flag | 内容 |
|---|---|
| `<前缀>_清晰度` | 结束时的清晰度整数百分比 |
| `<前缀>_用时` | 实际用时（秒，整数） |
| `<前缀>_档位` | 完美 / 普通 / 失败 |
| `事件结果` | 按项目惯例由 Runner 写入 |

---

## 八、分阶段实施计划

### P1 擦除内核可跑（不接剧本）

- [ ] [`VNFogMask`](Assets/Project/Scripts/VNEffects/Script/VNFogMask.cs)：掩码缓冲、`SetPixelData` 上传、`StrokeTo`/`Stamp`（从 [`VNPhotoDoodle`](Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs) 搬）
- [ ] `VNFogWipe.shader`：模糊 + 掩码 + 噪声边界
- [ ] 一个临时测试场景：铺一张 CG + 雾层 + 鼠标擦
- **验收**：能擦，边缘不规则，剪影透得出来，60 FPS 不掉帧

### P2 回雾与判定

- [ ] 边缘权重图预算 + 每帧衰减
- [ ] 随机雾团（反向 Stamp）+ 生成提示脉冲
- [ ] [`VNFogScore`](Assets/Project/Scripts/VNEffects/Script/VNFogScore.cs)：清晰度求和、阶段推进、三档判定
- [ ] `float[] _maskF` 内部缓冲（防 byte 截断）
- **验收**：把回雾调到 3%/s，实测「认真擦 50 秒左右到 80%」，与第三节数学吻合

### P3 模块化与 UI

- [ ] [`VNFogWipeModule`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeModule.cs) 继承 [`VNEventModule`](Assets/Project/Scripts/VNEffects/Script/VNEventModule.cs)，接 `VNEventContext` 参数
- [ ] [`VNTouchCursor`](Assets/Project/Scripts/VNEffects/Script/VNTouchCursor.cs) 接入（**Dispose 四处**）
- [ ] HUD：清晰度条 + 倒计时（最后 5 秒变红脉动，照 [`VNQuizModule`](Assets/Project/Scripts/VNEffects/Script/VNQuizModule.cs)）
- [ ] 自绘台词条 + 分阶段触发
- [ ] ESC 确认框 → 直接判「普通」
- [ ] `Done(结果)` + flag 回写 + `CancelForDebug` 清理
- **验收**：`event wipefog` 能从剧本拉起、三档结果都能走到、ESC 能退

### P4 资产、调参窗口与音效

- [ ] [`VNFogWipeDef`](Assets/Project/Scripts/VNEffects/Script/VNFogWipeDef.cs) 全字段 + [`VNGameConfig`](Assets/Project/Scripts/VNEffects/Script/VNGameConfig.cs) 登记 + Inspector 页签
- [ ] **[`VNFogTuneWindow`](Assets/Project/Scripts/VNEffects/Editor/VNFogTuneWindow.cs) 调参预览**（★ 刚性需求，见 1.3）：
      不进 Play Mode 试擦、实时看回雾、根据当前参数**算出预计通关秒数**
- [ ] [`VNProceduralTextures`](Assets/Project/Scripts/VNEffects/VNProceduralTextures.cs) 三个新贴图
- [ ] [`VNFogSfx`](Assets/Project/Scripts/VNEffects/Script/VNFogSfx.cs) 五个合成音效
- **验收**：改一个数字立刻能看出手感差别，不用进 Play Mode

### P5 装机、内容与文档

- [ ] [`VNFogWipeInstaller`](Assets/Project/Scripts/VNEffects/Editor/VNFogWipeInstaller.cs) 场景装机器
- [ ] [`VNScenarioSchema`](Assets/Project/Scripts/VNEffects/Editor/VNScenarioSchema.cs) 登记命令 + 下拉候选
- [ ] [`VNScenarioLinter`](Assets/Project/Scripts/VNEffects/Editor/VNScenarioLinter.cs) 两条检查
- [ ] 示例资产「浴室镜面」+ 一段示例剧本
- [ ] 配一个 [`VNTutorialDef`](Assets/Project/Scripts/VNEffects/Script/VNTutorialDef.cs) 教程
- [ ] 文档：`WhatAiDo.md` 新章节 / `CLAUDE.md` 组件表 / `HowToUse.md` 语法
- **验收**：新场景跑装机器后剧本直接能用

---

## 九、风险清单与保险丝

| # | 风险 | 保险丝 |
|---|---|---|
| 1 | **玩法寿命短**（最大风险） | 单场限制 45~60 秒；分阶段台词提供节奏点；教程解决前 10 秒的迷茫 |
| 2 | **参数一调不好就废**（见 1.3） | P4 的调参窗口 + 第三节的数学表；默认值按算出来的填 |
| 3 | 玩家不知道擦哪 | 雾保留底图剪影（决策 8）+ 教程 |
| 4 | 无脑甩鼠标秒通关 | 已接受（决策 4）。若实测确实太快，**唯一的补救旋钮是调小笔刷 + 调高回雾**，不加速度惩罚 |
| 5 | 边缘太圆滑显假 | `edgeNoise` 默认 0.35，不要调到 0 |
| 6 | **系统鼠标指针永久消失** | `VNTouchCursor.Dispose()`（[`Assets/Project/Scripts/VNEffects/Script/VNTouchCursor.cs:217`](Assets/Project/Scripts/VNEffects/Script/VNTouchCursor.cs#L217)） 四处调用，写进 code review checklist |
| 7 | 雾盖住对话框 | 台词条自绘（已在方案内） |
| 8 | byte 掩码数值截断 | 内部 `float[]` 缓冲 |
| 9 | 每帧 Apply 掉帧 | 掩码 256×144 + `SetPixelData` + 脏标记；实测参照 [`VNPhotoDoodle`](Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs) 的 768×576 都不掉帧 |
| 10 | 中途不能存档 | event 模块固有限制，可接受 |
| 11 | 触屏/手柄移植 | 本版只做鼠标；若移植需另设「摇杆移动虚拟光标」方案 |

---

## 十、被否决的方案与理由

| 方案 | 否决理由 |
|---|---|
| **拆出常驻 `VNFogWindow` 演出组件 + `fx fog` 命令** | 复用价值高，但要走存档兼容三处同步。决策 3 选了「正式模块」而非「两层」，本版从简；将来真需要纯演出起雾再补 |
| **速度惩罚（擦太快擦不净）** | 我推荐过，用户明确选了「轻松，擦到就掉」。尊重取向，改由参数承担难度（第三节） |
| **接部位框 / 立绘实时反应** | 决策 2 定了被动 CG。好处是模块不用破铁律、架构干净很多 |
| **目标区域隐藏 / 道具取舍** | 决策 6 只选了「面积就是挑战」，不做额外张力机制 |
| **雾用纯白不透明 Image** | 便宜但廉价，且玩家会迷茫。决策 8 选了剪影透出 |
| **掩码用全屏 1920×1080** | 纯浪费。雾本身模糊，256×144 双线性放大看不出差别，性能差 56 倍 |
| **Physics/RenderTexture 做擦除** | CPU 位图方案已有现成代码（[`VNPhotoDoodle`](Assets/Project/Scripts/VNEffects/Script/VNPhotoDoodle.cs)），且能直接求和统计清晰度；RT 方案要回读，反而更慢更麻烦 |
| **在雾上写字给她读（接 aitalk）** | 很有意思、也是本项目独有的组合，但属于「表达型」玩法，与本版的「对抗型」定位不同。**留作 v2 扩展** |

---

## 十一、工时估算

| 阶段 | 估时 |
|---|---|
| P1 擦除内核 | 3~4 h |
| P2 回雾与判定 | 2~3 h |
| P3 模块化与 UI | 4~5 h |
| P4 资产 + 调参窗口 + 音效 | 4~5 h |
| P5 装机 + 内容 + 文档 | 2~3 h |
| **合计** | **15~20 h**，其中**试玩调参预计还要额外 2~4 h**（这部分不可压缩） |

---

## 附：下一步

按项目工作规则，本文档只是**阶段 ① 之前的规划**。
用户发话后再：从 `main` 切 `feature/fog-wipe-minigame` → 按 P1~P5 实现 → 编译验证 → 报告改动 → 等验收。
**不主动 commit / push / 开 PR。**
